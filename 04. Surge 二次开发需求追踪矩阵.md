
> Surge 一键安装脚本二次开发的需求追踪矩阵。本文将已冻结的产品规则拆分为可验收需求，并连接后续技术模块、自动化测试和 Debian 13 真实验证。

**状态**：需求基线已冻结；技术设计与实现尚未开始。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 1. 使用规则

每项需求必须始终能回答：

```text
产品要求什么？
来源于哪份已确认文档？
怎样才算完成？
由哪些技术能力承接？
怎样自动测试？
如何在 Debian 13 VPS 上真实确认？
```

本文不规定函数名、目录或实现语言。表中的“候选技术承接”用于后续《开发模块及定位》和节点契约建立映射，不代表代码已经存在。

### 1.1 状态定义

| 类型 | 状态 | 含义 |
|---|---|---|
| 需求状态 | `frozen` | 产品行为已确认，开发必须遵守 |
| 需求状态 | `deferred` | 已决定延后，不作为 v1 完成条件 |
| 需求状态 | `out-of-scope` | v1 明确不实现 |
| 交付状态 | `not-designed` | 尚未形成技术承接方案 |
| 交付状态 | `designed` | 已映射到模块和节点契约 |
| 交付状态 | `implemented` | 实现完成，尚未完成测试 |
| 交付状态 | `tested` | 自动化或人工测试完成 |
| 交付状态 | `verified` | 已在目标 Debian 13 VPS 完成真实验证 |

当前所有 P0/P1 项目均为：`frozen / not-designed`。

### 1.2 优先级定义

| 优先级 | 含义 |
|---|---|
| P0 | v1 发布前必须完成并在目标环境验证 |
| P1 | v1 范围内的重要能力，可在 P0 核心闭环稳定后实现 |
| P2 | 明确可延后，不阻塞 v1 主链路 |

---

## 2. 上游来源与范围

| 标识 | 来源 | 作用 |
|---|---|---|
| MENU | 《最终结论版菜单树》 | 菜单入口和管理对象 |
| FLOW | 《交互问答流程稿》 | 输入、确认、取消、流程与结果 |
| CONFIG | 《配置文件结构草案》 | 主配置、派生资源、字段和安全原则 |
| RULE | 《联动规则与异常处理清单》 | 联动、异常、幂等性、回滚和结果规则 |
| PRODUCT | 《脚本模块拆分设计》 | 产品能力闭环 |
| DEC | 《04. Surge 二次开发技术决策基线》 | 已冻结技术决策 |

### 2.1 v1 范围

```text
真实验证环境：需求方独享的 Debian 13 amd64 VPS。
客户端：iOS Surge。
协议：Snell v6、AnyTLS、Hysteria2。
TLS：一个主域名，一张由 AnyTLS 与 Hysteria2 共用的证书。
证书：Let's Encrypt + acme.sh + HTTP-01 standalone。
```

---

## 3. P0 总览

| ID | 需求摘要 | 来源 | 候选技术承接 | 自动化验证 | Debian 13 真实验证 | 状态 |
|---|---|---|---|---|---|---|
| REQ-CORE-001 | 启动环境、权限和依赖检查 | FLOW, DEC | 环境检查 | 成功/失败分支 | 在 VPS 启动检查 | frozen / not-designed |
| REQ-UX-001 | 菜单、输入、取消和确认 | MENU, FLOW | 交互层 | 菜单与输入 smoke | 手动走读 | frozen / not-designed |
| REQ-CONFIG-001 | YAML 主配置、派生资源和权限 | CONFIG, DEC | Config + Python 工具 | schema/原子写入/权限 | 文件与权限检查 | frozen / not-designed |
| REQ-STATE-001 | 三类状态和 5 个修订版回滚 | CONFIG, RULE, DEC | 状态、修订、事务 | 修订与恢复测试 | 一次实际恢复 | frozen / not-designed |
| REQ-DEPLOY-001 | 单选/多选部署与预览确认 | MENU, FLOW, RULE | 部署编排 | 选择/计划/取消 | 单协议和组合部署 | frozen / not-designed |
| REQ-SNELL-001 | Snell v6 闭环 | FLOW, CONFIG, RULE | Snell 适配器与资源 | 内容/失败测试 | Surge 实连 | frozen / not-designed |
| REQ-ANYTLS-001 | AnyTLS 闭环 | FLOW, CONFIG, RULE | AnyTLS 适配器与资源 | 内容/失败测试 | Surge 实连 | frozen / not-designed |
| REQ-HY2-001 | Hysteria2 闭环 | FLOW, CONFIG, RULE | HY2 适配器与资源 | 参数/范围/失败测试 | Surge 实连 | frozen / not-designed |
| REQ-CERT-001 | 共享证书与续期恢复 | FLOW, CONFIG, RULE, DEC | 证书资源与事务 | 预检/失败/恢复 | 签发与续期 | frozen / not-designed |
| REQ-BIN-001 | 二进制版本、完整性和恢复 | RULE, DEC | Binary resource | 筛选/校验/替换 | 官方下载与版本检查 | frozen / not-designed |
| REQ-FW-001 | 防火墙 manual/auto 与传输区分 | FLOW, RULE, DEC | Firewall resource | TCP/UDP/range 计划 | 规则与监听检查 | frozen / not-designed |
| REQ-SVC-001 | 服务状态、启停、自启和日志 | MENU, FLOW, DEC | Systemd/service | 状态与确认 | systemd 操作 | frozen / not-designed |
| REQ-CONFIG-APPLY-001 | 配置修改事务式应用 | MENU, FLOW, RULE, DEC | 配置应用编排 | 影响/取消/恢复 | 修改并实连 | frozen / not-designed |
| REQ-UPDATE-001 | 更新、明确版本选择和回滚 | MENU, FLOW, RULE, DEC | 更新编排 | 下载/失败/恢复 | 更新或恢复 | frozen / not-designed |
| REQ-UNINSTALL-001 | 卸载与共享资源保护 | MENU, FLOW, RULE | 卸载编排 | 依赖/删除/失败 | 单协议卸载 | frozen / not-designed |
| REQ-SURGE-001 | Surge 导出、二维码和未同步提示 | FLOW, CONFIG, RULE | 导出层 | 条目/失败/脱敏 | iOS 导入连接 | frozen / not-designed |
| REQ-TRANS-001 | 事务、回滚、锁与中断 | RULE, DEC | Transaction layer | 故障注入 | 可控失败恢复 | frozen / not-designed |
| REQ-RESULT-001 | 结果、历史和修复提示 | FLOW, CONFIG, RULE | 渲染/历史/日志 | 结果与脱敏 | 人工可理解性 | frozen / not-designed |

---

## 4. P0 详细需求

### REQ-CORE-001：启动环境检查

**来源**：FLOW 2，DEC-003、DEC-004  
**优先级**：P0

脚本必须在进入主菜单前检查 root 权限、Debian 13 amd64、systemd、网络、Python 配置辅助依赖和防火墙工具能力。

**完成标准**

- 环境符合要求才进入菜单。
- 关键条件失败时输出明确原因并退出。
- 防火墙自动化工具未支持时，按规则提示或降级 manual，而不是错误地拒绝整个系统。
- Python 依赖缺失时明确提示或进入受控安装流程。

**验证**：模拟各类失败；目标 VPS 上完成启动检查。

---

### REQ-UX-001：菜单、输入与流程控制

**来源**：MENU，FLOW 1、2、3、11、12、13  
**优先级**：P0

产品必须提供部署服务、代理服务管理、证书管理、配置管理和退出五个主菜单入口。所有常驻选择使用编号菜单，高影响操作必须明确确认。

**完成标准**

- 返回上一级、返回主菜单、取消本次操作和退出脚本语义不同。
- 不使用 `[Y/n]` 或空输入默认值。
- 非法编号、空输入、中文逗号和格式错误必须提示后重新输入。
- 协议选择支持英文逗号与空格、自动去重排序；返回主菜单不能与协议混选。
- 在确认前取消，不写配置、不修改系统。

**验证**：输入解析与菜单 smoke；手动走读主要路径。

---

### REQ-CONFIG-001：唯一 YAML 主配置与安全规则

**来源**：CONFIG 1-3、18，PRODUCT 2.2，DEC-004、DEC-005  
**优先级**：P0

`/etc/proxy-installer/config.yaml` 是用户意图和已提交应用状态的唯一事实来源。runtime、systemd unit、Surge 和二维码均为派生资源。

**完成标准**

- YAML 只由 Python 配置辅助工具结构化读取、校验和写入；Bash 不得文本替换主配置。
- 写入经过临时文件校验和原子替换；失败保留旧配置。
- 主配置、修订快照及含密钥派生文件至少为 `600`。
- 密码、PSK 能支持重新导出，但普通状态与预览默认脱敏。
- 保存 `schema_version` 与 `config_revision`。

**验证**：schema、权限、原子失败和脱敏测试；VPS 文件检查。

---

### REQ-STATE-001：desired/applied/observed 与五次修订版

**来源**：CONFIG 1.2、5、15-16，RULE 13-16，DEC-006、DEC-007  
**优先级**：P0

系统必须区分用户希望的配置、最近成功应用的配置和最近观察到的真实系统状态。每次高影响操作成功后，保存一个成功修订版，最多保留最近 5 个。

**完成标准**

- 不混淆“想启用”“已应用”“当前正在运行”。
- 修订版含主配置、受影响 runtime/unit/Surge、二进制版本与资源状态摘要。
- 第 6 个成功修订产生后清理最旧成功修订；进行中快照不占长期修订位。
- 恢复历史修订也走预览、确认、快照、应用、健康验证和失败恢复。
- 详细过程写日志，不写入主 YAML。

**验证**：修订上限、恢复成功、恢复失败恢复当前状态；目标 VPS 完成一次低风险恢复。

---

### REQ-DEPLOY-001：部署选择、预检与最终确认

**来源**：MENU 2，FLOW 3-8，RULE 1、3，DEC-009  
**优先级**：P0

用户可选择三个协议的任意合法组合。系统先收集配置、检查依赖和冲突、展示最终影响；只有明确开始安装才执行真实操作。

**完成标准**

- 仅 Snell 不进入证书与续期流程。
- AnyTLS 或 Hysteria2 必须输入主域名并进行证书预检。
- 多协议共享统一部署计划，不维护组合专用流程。
- 预览动态展示协议、端口、证书、防火墙、自启、服务和客户端影响，敏感值不完整回显。
- 预检失败时可重试、返回修改、仅保留 Snell 或退出；不得提前写入成功状态。

**验证**：组合选择、Snell-only、预检失败与取消；VPS 上完成单协议和 TLS 组合部署。

---

### REQ-SNELL-001：Snell v6 部署与连接闭环

**来源**：FLOW 4-5、10-11、13，CONFIG 10，RULE 3、6、11，DEC-008、DEC-011、DEC-014  
**优先级**：P0

支持 Snell v6 的端口、PSK、客户端地址类型和固定 `mode=default`，并完成二进制、runtime、systemd、TCP 防火墙、健康检查与 Surge 导出。

**完成标准**

- Snell 只使用 TCP，不得额外放行 UDP。
- PSK、端口和 IP/域名按规则校验。
- 仅 Snell 且使用 IP 时不要求主域名。
- `mode` 保留字段，v1 仅支持 default。
- 服务成功必须确认 unit active、正确监听、配置/启动验证和版本，不以 restart 返回值代替。
- Surge 使用持久化参数生成。

**验证**：参数、runtime/unit/Surge、TCP 计划、失败恢复；Debian 13 + iOS Surge 真实连接。

---

### REQ-ANYTLS-001：AnyTLS 部署与连接闭环

**来源**：FLOW 4-5、10-11、13，CONFIG 8，RULE 3-4、11，DEC-008、DEC-011、DEC-016  
**优先级**：P0

支持 AnyTLS 端口、密码、Surge TFO 和 reuse 输出策略，使用共享 TLS 证书完成服务端配置、systemd 管理和 Surge 导出。

**完成标准**

- 必须引用唯一主域名下的共享证书。
- 端口冲突、域名或证书条件不满足时阻止应用或进入修复流程。
- TFO/reuse 若只影响 Surge，不无理由重启服务。
- 密码或端口变化后重生成运行配置、验证服务、重新导出 Surge。
- 仅开放实际需要的 TCP 端口。

**验证**：参数、证书引用、runtime/unit/Surge、冲突与失败恢复；真实证书与 Surge 连接。

---

### REQ-HY2-001：Hysteria2 部署与连接闭环

**来源**：FLOW 4-5、10-11、13，CONFIG 9，RULE 3、5、11，DEC-008、DEC-011、DEC-016、DEC-017  
**优先级**：P0

支持 Hysteria2 主端口、密码、端口跳跃、跳跃间隔、Gecko、固定分片参数和 Surge `download-bandwidth`。

**完成标准**

- 主端口不得落入跳跃范围；资源冲突必须拒绝。
- 主端口和跳跃范围均按 UDP 处理，不额外开放 TCP。
- Gecko 启用时使用既定分片参数，密码安全保存并脱敏。
- 跳跃、Gecko、带宽、端口和密码变化正确触发重建、验证和导出。
- 健康验证通过后才提交成功状态。

**验证**：端口范围、冲突、Gecko、UDP/range 计划、失败恢复；Debian 13 + iOS Surge 连接验证。

---

### REQ-CERT-001：共享证书、续期与恢复

**来源**：MENU 4，FLOW 6、9、12，CONFIG 11，RULE 3、10，DEC-010、DEC-016  
**优先级**：P0

AnyTLS 与 Hysteria2 共用一个主域名和一张 Let's Encrypt 证书。申请、更新、续期、状态、timer 和清理均是独立管理能力，且必须使用统一事务。

**完成标准**

- 使用 acme.sh、HTTP-01 standalone；检查 DNS、域名/IP、TCP 80 与防火墙风险。
- TCP 80 被已识别服务占用时，展示影响、明确确认后才临时停止，结束后恢复。
- 申请/续期失败保留旧活动证书和正常 TLS 服务。
- 候选证书仅在校验、切换、关联服务更新和健康检查成功后成为活动证书。
- timer 可查看、启用和停用；续期后更新 TLS 服务。
- 清理前检查依赖；仍被 TLS 服务使用时强确认。

**验证**：预检、80 占用、候选切换、服务失败恢复、timer；真实签发与一次续期验证。

---

### REQ-BIN-001：二进制版本与完整性

**来源**：RULE 8，DEC-013、DEC-014、DEC-015  
**优先级**：P0

每种二进制必须具备官方来源、架构映射、压缩格式、版本检测、可信完整性验证和替换恢复策略。安装/更新展示最新三个具体兼容版本并要求用户选择。

**完成标准**

- 候选显示形如 `vX.Y.Z（最新稳定版，发布于 YYYY-MM-DD）`。
- 只展示 Debian 13 amd64、当前能力兼容且可校验完整性的正式版本。
- 下载后校验、解压、确认可执行文件及版本，再替换活动二进制。
- 失败恢复旧二进制、旧配置和旧服务。
- 共享二进制更新先展示影响并满足所有消费者兼容性。
- v1 不支持脚本本体在线更新。

**验证**：版本筛选、架构映射、校验失败、解压失败、替换失败；真实官方下载与版本检查。

---

### REQ-FW-001：防火墙模式和传输准确性

**来源**：FLOW 4.5，RULE 2.5、12，DEC-017  
**优先级**：P0

用户选择 `manual` 或 `auto`；系统按 TCP 端口、UDP 端口和 UDP 范围分别计算、展示和应用规则。

**完成标准**

- manual 不改规则，只输出准确本机与云安全组提示。
- auto 仅对已支持工具执行；v1 优先 ufw 和 nftables。
- 无法识别/支持的环境降级 manual 或停止自动操作，不猜测规则。
- 新端口、跳跃范围和证书 TCP 80 都按实际传输处理。
- v1 默认不自动删除旧规则，结果说明遗留情况。

**验证**：TCP/UDP/range 计划、manual、工具检测、失败降级；实际规则和监听检查。

---

### REQ-SVC-001：服务生命周期与健康观察

**来源**：MENU 3，FLOW 11，CONFIG 12，DEC-006、DEC-011  
**优先级**：P0

用户可按单服务或全部已安装服务查看状态、启停、设置自启和查看有限日志。系统保留最近观察状态摘要。

**完成标准**

- 未安装服务明确显示，不执行假动作。
- 启停、自启、重启前展示目标并确认。
- 日志只展示有限摘要，例如 50/100 行，不默认无限跟随。
- 部署、更新与配置应用使用严格健康检查：unit active、正确监听、配置/启动验证、日志和版本。
- observed 状态有检查时间，不伪装为实时事实。

**验证**：目标展开、状态映射、确认、日志行数；Debian 13 systemd 实际操作。

---

### REQ-CONFIG-APPLY-001：配置修改的事务式应用

**来源**：MENU 5，FLOW 13，RULE 1、4-7，DEC-007、DEC-009  
**优先级**：P0

全局、AnyTLS、Hysteria2 和 Snell 配置采用单次变更流程；不立即生效，也不提供脱离上下文的“应用全部变更”。

**完成标准**

- 统一流程：读旧值 → 输入 → 校验 → 影响分析 → 预览 → 确认 → 快照 → 应用 → 健康验证 → Surge 导出 → 结果。
- 放弃或返回修改不写配置、不改系统。
- 端口、密码/PSK、域名、地址类型、跳跃、Gecko、带宽、防火墙和自启按各自影响处理。
- 域名变更保护旧证书与旧服务；Hysteria2 主端口不得进入跳跃范围。
- 仅影响 Surge 的字段不无理由重启服务。
- 服务端成功而导出失败时记录客户端未同步，不回滚服务端。

**验证**：字段校验、计划、取消、冲突、服务失败回滚、仅导出字段；VPS 上完成一次真实修改与连接检查。

---

### REQ-UPDATE-001：受控更新与批量停止策略

**来源**：MENU 3，FLOW 11.7，RULE 8，DEC-009、DEC-014  
**优先级**：P0

用户可更新单项或全部已安装服务。更新展示当前版本、三个具体候选版本和影响，失败后恢复旧资源。

**完成标准**

- 只提供符合 REQ-BIN-001 的候选版本。
- 更新前快照，新版本校验和版本检查通过后才替换。
- 更新后必须健康验证；失败恢复旧二进制、配置与服务。
- 批量更新逐个执行；任一失败后停止后续更新，并汇总成功、失败、未执行项。
- 成功后才更新 applied 版本摘要与历史。

**验证**：成功、下载失败、校验失败、服务失败恢复、批量停止；真实选择一个版本完成更新或恢复。

---

### REQ-UNINSTALL-001：卸载与共享资源保护

**来源**：MENU 3，FLOW 11.8，RULE 9，DEC-009、DEC-016、DEC-017  
**优先级**：P0

用户可卸载单个或全部协议。操作先展示停止、删除、保留与客户端影响；未完成步骤不得报告成功。

**完成标准**

- 卸载前强确认并展示受影响服务、文件、二进制、Surge 与共享资源。
- 单协议卸载停止/禁用服务、删除派生资源、更新配置与导出。
- 卸载 Snell 不影响证书/timer。
- 卸载最后 TLS 协议后不自动删除证书/timer，仅提示进入证书管理。
- v1 不自动删除旧防火墙规则。
- 卸载事实成功但 Surge 导出失败时，记录“已卸载、客户端未同步”。

**验证**：单协议、最后 TLS、Snell、删除失败、导出失败和全部卸载；真实卸载非关键协议且确认其他服务仍可用。

---

### REQ-SURGE-001：Surge 导出与客户端同步状态

**来源**：FLOW 10、11.9，CONFIG 13、18，RULE 11，DEC-005、DEC-009  
**优先级**：P0

Surge 配置仅由当前持久化配置派生。用户可不重新输入参数地重导节点、配置片段和二维码。

**完成标准**

- 新增、卸载、端口、密码/PSK、Hysteria2 参数、Snell 地址类型、域名或导出命名变化后触发重导。
- 状态/日志查询、服务启停和 timer 启停不强制重导。
- 导出使用当前启用协议的真实参数；普通展示默认脱敏。
- 导出失败不回滚已验证服务端变更，单独记录并指导重新导出。
- iOS Surge 必须能用导出的节点完成真实连接验证。

**验证**：协议条目、组合导出、触发、失败和脱敏；iOS Surge 导入及连接。

---

### REQ-TRANS-001：统一事务、回滚、锁与中断

**来源**：RULE 1、13-16，PRODUCT 2.5、13，DEC-007、DEC-009、DEC-012  
**优先级**：P0

部署、配置应用、更新、卸载、证书申请/切换/续期与历史修订恢复共用统一事务。高影响操作同时只能运行一个。

**完成标准**

- 统一执行：计划 → 校验 → 预览 → 确认 → 快照 → 执行 → 健康验证 → 提交/恢复。
- 下载、runtime、unit、服务启动和健康检查失败等关键异常有明确状态。
- 可恢复失败优先恢复旧 runtime、二进制、服务和配置状态。
- 导出、防火墙同步或历史写入失败等不宜强回滚的情况保留成功事实并记录待处理项。
- 回滚失败标记状态不确定并给出可操作修复提示。
- Ctrl+C、异常中断和重复执行不能静默破坏服务；全局锁阻止并发。

**验证**：故障注入、恢复顺序、锁、中断和回滚失败；目标 VPS 一次低风险可控失败恢复。

---

### REQ-RESULT-001：统一结果、历史与修复指引

**来源**：FLOW 8、10-13，CONFIG 15、18，RULE 15-16，DEC-006、DEC-012  
**优先级**：P1

高影响操作结束后，用户必须看懂已完成、未完成、当前状态可信度和下一步。系统保存有限历史摘要和操作编号。

**完成标准**

- 统一输出：执行结果、已完成、未完成、当前状态、后续建议。
- 区分成功、部分成功、失败、成功回滚和状态不确定。
- 服务端成功但 Surge 未导出时明确说明客户端未同步。
- 普通预览、状态、错误默认脱敏；仅用户明确请求客户端配置时输出必要完整参数。
- 历史保留摘要；详细输出写日志，不写 YAML。

**验证**：结果分类、脱敏、历史保留、错误提示；需求方手动确认可理解性。

---

## 5. P1/P2 与明确非范围

| ID | 优先级 | 内容 | 结论 | 状态 |
|---|---|---|---|---|
| REQ-CERT-MGMT-001 | P1 | 完整证书管理菜单 | 主链路稳定后实现 | frozen / not-designed |
| REQ-CONFIG-VIEW-001 | P1 | 分组查看当前配置 | 默认脱敏，显示值来源与状态摘要 | frozen / not-designed |
| REQ-HISTORY-001 | P1 | 有限操作历史摘要 | 与修订版不同，用于查看发生过什么 | frozen / not-designed |
| REQ-HOOK-001 | P2 | 自定义 hooks | 仅预留结构，v1 不执行 | deferred / not-designed |
| REQ-MULTI-DOMAIN-001 | P2 | 多域名/每协议独立证书 | v1 不支持 | out-of-scope / not-designed |
| REQ-ARM64-001 | P2 | arm64 验证 | 不纳入 v1 已验证范围 | deferred / not-designed |
| REQ-SCRIPT-UPDATE-001 | P2 | 脚本在线自更新 | v1 不支持 | out-of-scope / not-designed |
| REQ-SNELL-MODE-001 | P2 | 非 default mode | 结构保留，v1 延后 | deferred / not-designed |
| REQ-FW-CLEANUP-001 | P2 | 自动删除旧防火墙规则 | v1 只提示 | deferred / not-designed |

---

## 6. 进入技术设计的检查

《开发模块及定位》与节点契约必须逐项承接 P0 需求：

```text
[x] 产品、交互、配置和异常规则已确认来源。
[x] Debian 13 amd64 真实验证边界已确认。
[x] Bash 主体与 Python YAML 工具边界已确认。
[x] 三类状态、五次修订版和统一事务已确认。
[x] 协议注册表、二进制、证书、防火墙和健康检查原则已确认。
[x] P0/P1 已编号并具有验收方式。
[ ] 每项 P0 映射到正式技术模块与节点契约。
[ ] 首批节点契约完成。
[ ] 本地代码工作区、测试入口和开发规范建立。
```

里程碑交付时，应更新每项需求的交付状态、自动化测试、真实验证记录、已知限制和是否可进入下一里程碑。只有 P0 在目标 Debian 13 VPS 达到 `verified`，才能作为发布版宣称完成。
