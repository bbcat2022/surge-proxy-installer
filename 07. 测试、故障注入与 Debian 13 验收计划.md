
> 本文档定义 Surge 一键安装脚本二次开发的验证策略。目标不是追求“测试数量很多”，而是明确每项需求在何处验证、关键失败如何演练，以及何时才可以说脚本能在需求方的 Debian 13 VPS 上真实使用。

**状态**：开发前测试与验收基线。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 一、目标与原则

### 1.1 验证目标

每项 P0 需求必须从三个层面获得证据：

```text
逻辑正确：输入、输出、状态与规则符合预期。
失败可控：关键失败不会误报成功、破坏旧服务或丢失恢复信息。
真实可用：在 Debian 13 VPS 与 iOS Surge 的目标环境中实际成立。
```

“所有测试通过”必须附带测试层次。以下结论不等价：

```text
unit passed：小函数或纯逻辑正确。
node passed：单个节点契约成立。
integration passed：多个节点组合正确。
dry-run passed：计划路径正确，未执行真实系统改动。
fault passed：指定失败路径得到预期处理。
verified：在目标 Debian 13 VPS 完成真实验证。
```

### 1.2 不反复重建 VPS 的原则

本项目仅供需求方个人使用，真实环境为独享 Debian 13 amd64 VPS。测试策略不要求每次候选版本重装系统。

```text
优先在隔离目录、mock、dry-run 和故障替身中验证逻辑；
真实 VPS 只验证无法由模拟证明的能力；
真实动作前必须有预览、快照、操作编号和恢复路径；
每次真实验证记录实际覆盖范围；
不把“本次未验证”写成“已支持”。
```

### 1.3 责任分工

| 角色 | 负责内容 |
|---|---|
| Codex 开发阶段 | 实现节点测试、集成测试、dry-run、故障注入和测试报告 |
| 需求方 | 确认用户可理解性、选择真实验证时机、在 iOS Surge 上确认实际连接体验 |
| 真实 VPS | 验证 systemd、网络、证书、二进制、监听和真实客户端连接 |

---

## 二、测试层次与准入规则

### 2.1 测试金字塔

```text
Unit
├── 校验器、格式化、状态转换、版本筛选

Node
├── YAML 工具、修订管理、事务、注册表、资源原语、协议适配器

Integration
├── 计划 + 适配器 + writer + 模拟资源

Fault Injection
├── 下载失败、写入失败、服务失败、回滚失败、中断等

Smoke
├── 开发 CLI 和正式菜单的主要用户路径

Debian 13 Real Acceptance
└── 真实二进制、systemd、网络、证书、Surge 连接
```

### 2.2 开发变更与最低测试

| 修改范围 | 最低必须运行 |
|---|---|
| Core 校验或渲染 | unit + 直接 node 测试 |
| Python YAML 工具 | unit + N1 node + 原子写入失败测试 |
| Config/State/Revision | node + 修订恢复集成测试 |
| Registry 或协议适配器 | node + 相关协议集成测试 |
| Binary、Certificate、Firewall、Systemd | node + 相关故障注入 + dry-run |
| Transaction 或编排器 | integration + 故障注入 + smoke |
| 菜单和交互 | smoke + 受影响编排集成测试 |
| 真实系统资源逻辑 | 上述测试 + 有计划的 Debian 13 目标验证 |

### 2.3 允许进入下一层的条件

```text
Unit → Node：纯逻辑与输入边界通过。
Node → Integration：节点契约满足，副作用可 mock 或 dry-run。
Integration → Fault：正常组合路径通过，失败点可注入。
Fault → Smoke：关键失败不误报成功，恢复或 dirty 结果明确。
Smoke → Real：用户流程与预览/确认语义正确，真实动作范围已确认。
Real → Verified：目标 VPS 与 iOS Surge 结果符合 P0 验收标准。
```

---

## 三、本地与隔离测试环境

### 3.1 隔离目录原则

本地测试不得使用真实路径，例如：

```text
/etc/proxy-installer
/etc/systemd/system
/usr/local/bin
```

测试应使用临时目录模拟：

```text
主配置目录；
runtime 目录；
修订与备份目录；
二进制缓存与活动路径；
systemd unit 目录；
Surge 导出目录；
日志目录。
```

测试结束后应清理临时目录；失败时保留足够诊断信息，但不得泄露敏感测试密钥。

### 3.2 外部命令替身

在 node、integration 和 fault 测试中，以下外部命令应可替换或模拟：

```text
systemctl
journalctl
ss
ufw
nft
curl 或下载工具
acme.sh
协议二进制自身的版本检查命令
```

替身必须能模拟：

```text
成功；
返回非零；
部分成功；
超时或网络不可用；
执行后状态与预期不一致；
恢复动作也失败。
```

### 3.3 dry-run 规则

dry-run 用于验证计划、影响、调用顺序和用户展示，不用于证明真实部署成功。

dry-run 测试必须确认：

```text
不会写主配置；
不会替换活动 runtime/unit/binary；
不会调用 systemctl；
不会修改防火墙；
不会申请证书；
输出明确标识为 dry-run；
预览资源与真实计划一致。
```

---

## 四、首批节点测试计划

### 4.1 N1：Python YAML 配置工具

| 场景 | 层次 | 预期 |
|---|---|---|
| 合法 YAML 读取与查询 | unit/node | 返回预期字段，不泄露敏感值 |
| YAML 语法损坏 | node | 拒绝并保留原文件 |
| schema 不兼容 | node | 返回 validation error，不写入 |
| 合法 patch | node | 临时校验后原子替换 |
| 非法路径或类型 | node | 拒绝 patch，不改原文件 |
| 临时文件写失败 | fault | 原文件不变 |
| 原子替换失败 | fault | 原文件不变，错误明确 |
| dry-run | node | 不写文件 |

**真实验证**：在 Debian 13 的受控测试目录创建、查询、修改和恢复样例配置；不触碰真实活动服务。

### 4.2 N2：配置、状态与修订管理

| 场景 | 层次 | 预期 |
|---|---|---|
| 空状态初始化 | node | 生成有效最小配置与安全权限 |
| desired/applied/observed 分别更新 | node | 三类状态不互相覆盖 |
| 脱敏状态展示 | unit/node | 密码、PSK、私钥不回显 |
| 五个修订保存 | node | 五个均可列出 |
| 第六个修订保存 | integration | 自动清理最旧修订 |
| 修订快照创建失败 | fault | 高影响事务不能继续 |
| 历史摘要写入 | node | 不含完整敏感信息 |

**真实验证**：在受控测试目录确认权限、修订上限和摘要展示。

### 4.3 N3：事务、快照与回滚

| 场景 | 层次 | 预期 |
|---|---|---|
| 合法计划完成 | integration | 健康验证后才提交 applied 与成功修订 |
| 计划缺少关键恢复动作 | node | 执行前拒绝 |
| 快照失败 | fault | 不修改活动资源 |
| 第 N 步失败 | fault | 停止后续，逆序恢复 |
| 健康检查失败 | fault | 不提交 applied，恢复旧资源 |
| Surge 导出失败 | fault | 保留服务端成功，记录未同步 |
| 锁冲突 | node | 第二操作被拒绝并显示操作上下文 |
| 中断 | fault | 记录最后步骤，恢复或 dirty 明确 |
| 回滚失败 | fault | 结果为 dirty，含修复建议 |

**真实验证**：只在可控、低风险场景中演练一次服务重启失败或候选配置失败后的恢复；操作前必须有有效快照。

### 4.4 N4：协议注册表

| 场景 | 层次 | 预期 |
|---|---|---|
| 三协议必填字段 | node | 启动时校验通过 |
| 未注册协议 | node | 明确拒绝 |
| Snell 网络声明 | node | 仅 TCP |
| AnyTLS 网络/TLS 声明 | node | TCP + 共享证书 |
| Hysteria2 网络声明 | node | UDP + 可选 UDP range |
| 敏感参数标记 | node | 密码/PSK/Gecko 被标记 |
| 缺字段注册项 | fault | 注册表校验失败 |

**真实验证**：不需要独立真实验证；由协议部署和防火墙验收间接覆盖。

### 4.5 N5：二进制资源管理

| 场景 | 层次 | 预期 |
|---|---|---|
| 候选版本筛选 | unit/node | 仅三个具体、兼容的正式版本 |
| 架构不匹配 | node | 不展示或拒绝安装 |
| 校验和正确 | node | 候选可进入安装准备 |
| 校验和失败 | fault | 不替换旧二进制 |
| 解压失败 | fault | 不替换旧二进制 |
| 版本检查失败 | fault | 不替换旧二进制 |
| 共享二进制冲突 | integration | 不提供不兼容候选 |
| 服务健康失败 | fault | 由事务恢复旧版本 |

**真实验证**：从官方来源选择一个明确版本，在 Debian 13 下载、校验、解压、检测版本；仅在已具备恢复路径时替换活动版本。

### 4.6 N6：systemd 服务原语

| 场景 | 层次 | 预期 |
|---|---|---|
| unit 原子安装 | node | 新 unit 生效前旧 unit 完整保留 |
| daemon-reload 失败 | fault | 错误返回给事务层 |
| start/restart 失败 | node/fault | 不报告 active |
| active/enabled 分别读取 | node | 两种状态不混淆 |
| 有限日志读取 | node | 限制行数且可读 |
| dry-run | node | 不调用 systemctl |
| 恢复旧 unit | integration | reload 后能回到原描述 |

**真实验证**：只对脚本创建的测试 unit 或协议 unit 进行状态、启停、自启和日志测试；不得操作无关系统服务。

### 4.7 N7：防火墙资源管理

| 场景 | 层次 | 预期 |
|---|---|---|
| Snell 资源计划 | node | 仅 TCP 单端口 |
| AnyTLS 资源计划 | node | 仅 TCP 单端口 |
| Hysteria2 主端口 | node | 仅 UDP 单端口 |
| Hysteria2 跳跃 | node | UDP range |
| manual | node | 不执行规则修改，输出指引 |
| 工具未支持 | node | 降级 manual，不猜命令 |
| auto 写入失败 | fault | 返回可读失败，不误报成功 |
| 旧规则 | integration | 默认不生成自动删除动作 |

**真实验证**：先在不影响现有连接的端口上验证规则计划与实际监听一致；云安全组仅人工确认，不以本机测试替代。

### 4.8 N8：Snell v6 适配器

| 场景 | 层次 | 预期 |
|---|---|---|
| 域名地址类型 | node | 构建正确 runtime/unit/Surge 条目 |
| IP 地址类型 | node | 不要求 TLS 域名 |
| 非法端口/PSK/IP/domain | unit/node | 明确 validation error |
| mode 非 default | node | v1 拒绝 |
| runtime 内容 | node | listen、PSK、mode 与既定字段正确 |
| unit 内容 | node | 使用受控路径 |
| 网络资源 | node | 仅 TCP |
| 副作用隔离 | node | 不触发 systemd、防火墙、证书或配置写入 |

**真实验证**：由后续 Snell 部署编排完成，使用 iOS Surge 实际连接。

---

## 五、编排与用户流程测试计划

首批节点完成后，必须覆盖以下组合流程。

### 5.1 部署流程

| 场景 | 自动化验证 | 真实验证 |
|---|---|---|
| 仅 Snell | 跳过证书、生成 TCP 计划、预览与确认 | Snell 服务与 Surge 连接 |
| 仅 AnyTLS | 证书预检、共享证书引用、TCP 计划 | 证书、服务与 Surge 连接 |
| 仅 Hysteria2 | 证书预检、UDP/range 计划 | 证书、服务与 Surge 连接 |
| AnyTLS + Hysteria2 | 一个证书、两个 TLS 服务 | 两服务共用证书并连接 |
| 三协议组合 | Snell 不参与证书、三类资源正确 | 分别检查连接 |
| 预检失败 | 重试/返回/仅保留 Snell/退出 | 可在真实 DNS 问题修复后验证 |
| 安装确认取消 | 不写配置、不改系统 | 在安全环境手动确认 |

### 5.2 配置应用流程

| 场景 | 关键断言 |
|---|---|
| AnyTLS/Hysteria2/Snell 端口变更 | 冲突检查、正确防火墙计划、服务验证、Surge 重导 |
| Hysteria2 主端口进入跳跃范围 | 拒绝，不改资源 |
| 密码或 PSK 变更 | 服务端更新、Surge 重导、敏感值不在预览泄露 |
| TFO/reuse 变更 | 只影响客户端时不重启服务 |
| Snell 地址类型变更 | Surge 重导，域名/IP 校验正确 |
| 主域名变更 | 候选证书、旧服务保护、失败恢复 |
| 用户取消 | 主配置、运行资源和修订版均不变 |

### 5.3 更新、卸载和修订恢复

| 场景 | 关键断言 |
|---|---|
| 单协议更新 | 明确版本选择、备份、健康检查、版本提交 |
| 更新服务失败 | 旧二进制、旧配置、旧服务恢复 |
| 批量更新失败 | 停止后续、汇总成功/失败/未执行 |
| 卸载 Snell | 不影响证书与 timer |
| 卸载最后 TLS 协议 | 不自动删除证书和 timer |
| 历史修订恢复 | 预览、当前快照、恢复、健康验证、新修订 |
| Surge 导出失败 | 事实状态正确，客户端未同步明确 |

---

## 六、故障注入矩阵

以下故障不是全部都需要在真实 VPS 上制造；多数应通过替身或隔离目录注入。

| 故障点 | 注入层 | 预期结果 | 是否需要真实验证 |
|---|---|---|---|
| Python/YAML 缺失或损坏 | node | 阻止配置写入，错误明确 | 否 |
| 主配置临时写入失败 | fault | 保留旧配置 | 否 |
| 修订快照失败 | fault | 高影响操作不开始 | 否 |
| 锁已占用 | node | 第二操作被拒绝 | 否 |
| 二进制下载失败 | fault | 不改活动二进制 | 否 |
| 二进制校验失败 | fault | 不替换旧版本 | 否 |
| 解压/版本检查失败 | fault | 不替换旧版本 | 否 |
| runtime 写入失败 | fault | 保留旧 runtime | 否 |
| unit 写入或 reload 失败 | fault | 保留或恢复旧 unit | 否 |
| 防火墙写入失败 | fault | 状态明确，按计划继续或提示 | 否 |
| 证书预检失败 | integration | 不改活动证书和服务 | 可选 |
| 证书切换后服务失败 | fault | 恢复旧活动证书和服务 | 可控后验证一次 |
| 服务 restart 失败 | fault | 不提交 applied，恢复旧资源 | 可控后验证一次 |
| is-active 失败 | fault | 视为部署/更新失败 | 可控后验证一次 |
| Surge 导出失败 | fault | 服务端成功保留，记录未同步 | 否 |
| history 写入失败 | fault | 不误报完整成功，按规则记录 | 否 |
| rollback 失败 | fault | dirty + 操作编号 + 修复提示 | 否 |
| Ctrl+C 中断 | fault | 恢复或 dirty，不丢旧备份 | 可控后验证一次 |

每新增一个高影响资源步骤，都必须在本表增加相应失败点和预期状态。

---

## 七、Debian 13 真实验收计划

### 7.1 验收前置条件

```text
需求方独享 Debian 13 amd64 VPS；
具有 root 权限；
具备可指向该 VPS 的测试主域名；
已确认云安全组可按需要放行 TCP 80、协议 TCP/UDP 端口；
可使用 iOS Surge；
当前活动服务、配置和二进制均已创建可恢复快照；
本次验收范围与风险已提前说明。
```

### 7.2 验收步骤

#### A. 环境与配置底座

```text
1. 运行启动检查。
2. 初始化主配置并检查权限。
3. 执行一次无副作用 dry-run。
4. 保存并列出配置修订。
5. 在隔离配置范围内执行一次修订恢复。
```

**预期**：不影响无关系统服务；状态、权限与修订结果符合文档。

#### B. 二进制与系统服务

```text
1. 从官方来源查看三个具体候选版本。
2. 选择一个版本并验证下载、完整性、解压和版本检测。
3. 写入脚本管理的 unit。
4. 检查 daemon-reload、active、enabled、日志摘要。
5. 确认正确协议/端口监听。
```

**预期**：不把 restart 返回值误认为健康；版本与监听均可观察。

#### C. 协议和客户端连接

```text
1. 部署 Snell v6 并使用 iOS Surge 连接。
2. 申请共享证书并部署 AnyTLS，使用 Surge 连接。
3. 部署 Hysteria2，检查 UDP 与端口跳跃设置，使用 Surge 连接。
4. 组合部署时确认三种协议互不覆盖，TLS 协议共用同一证书。
```

**预期**：每个协议的 Surge 输出、服务状态、监听和真实连接一致。

#### D. 生命周期和恢复

```text
1. 修改一个低风险配置字段并确认预览、应用与 Surge 重导。
2. 选择一个具体二进制版本完成受控更新。
3. 演练一个可控失败，例如候选配置导致健康检查失败。
4. 确认旧服务或明确 dirty 状态被保留。
5. 卸载一个非关键协议，并确认共享资源未被误删。
```

**预期**：结果页面能清楚说明已完成、未完成、当前状态与下一步。

### 7.3 验收记录模板

```markdown
# Debian 13 Acceptance Record: <版本或里程碑>

日期：
脚本版本：
VPS：Debian 13 amd64
测试域名：
iOS Surge 版本：

## 本次覆盖需求 ID

## 前置快照

## 执行步骤与实际结果

## Surge 连接结果

## 失败与恢复记录

## 未测试或延期项目

## 结论
- pass / partial / fail
```

---

## 八、发布与回归门槛

### 8.1 候选验收版

可标记为“候选验收版”的最低条件：

```text
范围内 P0 已完成技术设计与实现；
相关 unit、node、integration 测试通过；
关键故障注入完成；
主要菜单 smoke 通过；
已知限制明确；
Debian 13 真实验收计划已准备。
```

### 8.2 发布版

可标记为“发布版”的最低条件：

```text
所有 P0 达到 verified，或明确通过需求变更延期；
Snell、AnyTLS、Hysteria2 在 Debian 13 与 iOS Surge 真实连接通过；
共享证书、服务健康、版本选择和防火墙行为已验证；
至少一条关键失败恢复路径已验证；
打包、解包和基本 smoke 测试通过；
发布说明列出实际验证范围和已知限制。
```

### 8.3 回归规则

| 改动类型 | 需要重跑 |
|---|---|
| YAML/schema/状态模型 | N1、N2、N3 及相关恢复测试 |
| Registry/协议参数 | 相应适配器、计划、防火墙与 Surge 测试 |
| 二进制策略 | N5、更新故障、协议健康验证 |
| Certificate | 证书流程、TLS 协议、服务恢复 |
| Firewall | N7、受影响协议网络计划、真实监听检查 |
| Transaction | 所有受影响编排器、关键故障注入 |
| 菜单 | 受影响 smoke 与用户流程走读 |

---

## 九、进入 Codex 本地开发前的检查

```text
[x] 测试层次已定义。
[x] 首批节点已具有必测场景。
[x] 高风险故障点已列出预期结果。
[x] Debian 13 的真实验证方式已定义，且不要求反复重建 VPS。
[x] 候选验收版与发布版的门槛已定义。
[ ] Codex 本地开发交接清单已创建。
[ ] 首个节点 N1 的具体开发任务已创建。
```

下一份开发前文档应为《09. Codex 本地开发交接清单》：它将把必读文档、工作区规则、任务粒度、测试命令原则和真实 VPS 操作授权统一交给后续 Codex 开发阶段。
