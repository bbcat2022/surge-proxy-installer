
> 本文档是 Surge 一键安装脚本二次开发的技术承接蓝图。它以产品需求基线、技术决策基线和需求追踪矩阵为上游约束，定义模块边界、调用方向、资源所有权、事务责任和测试责任。本文不实现代码，也不替代节点级函数契约。

**状态**：技术设计基线，待节点契约与代码实现承接。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 一、文档定位

### 1.1 它解决什么问题

产品需求文档已经说明“产品要做什么”；本文解决“开发时由谁实现、谁不能实现、怎样组合才不会破坏产品规则”。

本文回答：

```text
每类代码和系统动作属于哪个模块？
模块之间允许怎样调用？
谁拥有主配置、真实系统资源和最终成功状态？
协议定制参数怎样统一描述？
部署、更新、卸载、证书续期怎样共享同一个事务框架？
每个模块要通过哪些测试，何时需要 Debian 13 真实验证？
```

### 1.2 它不解决什么问题

本文不重新决定：

```text
菜单文案；
用户流程；
协议是否支持；
域名、证书与防火墙的产品规则；
函数名、具体 Bash 语法和每个文件的完整实现；
测试框架的内部细节。
```

这些内容分别由需求基线、技术决策基线和后续节点契约负责。

### 1.3 上游文档与冲突处理

| 优先级 | 文档 | 用途 |
|---|---|---|
| 1 | 产品需求基线文档 | 用户可见行为、范围、交互、数据与异常规则 |
| 2 | 《04. Surge 二次开发技术决策基线》 | 已冻结技术取舍 |
| 3 | 《05. Surge 二次开发需求追踪矩阵》 | P0/P1 需求、验收和交付状态 |
| 4 | 本文 | 模块、调用方向和技术承接 |
| 5 | 节点契约 | 具体输入、输出、错误和测试 |

若本文无法满足上游规则，必须提出需求变更或技术契约变更；不得在代码中自行弱化要求。

---

## 二、总体架构

### 2.1 运行时组成

```text
Bash 主体
├── 正式终端菜单与开发 CLI
├── 业务编排与操作事务
├── systemd / 防火墙 / 证书 / 下载等系统资源调用
├── 运行配置、unit 与 Surge 的生成和安装
└── 测试入口、日志与结果展示

Python 配置辅助工具
├── YAML 读取
├── schema 校验
├── 结构化查询与 patch 合并
└── 临时 YAML 校验与原子替换
```

Python 是受控辅助工具，不是第二套部署程序。它不调用 `systemctl`、不申请证书、不修改防火墙，也不决定操作是否成功。

### 2.2 分层与调用方向

```text
CLI / 正式菜单
        ↓
交互流程与展示
        ↓
业务编排器
        ↓
操作事务引擎
        ↓
协议适配器 + 配置状态层 + 资源原语
        ↓
系统与文件资源
```

各层可读取下层返回的结果，但不得绕过中间边界直接修改不属于自己的资源。

### 2.3 模块树

```text
proxy-installer/
├── bin/
│   ├── proxy-installer.sh             正式用户入口
│   └── dev.sh                         仅开发与自动化调用入口
│
├── lib/
│   ├── core/                          常量、日志、渲染、输入、校验、锁
│   ├── config/                        Bash 配置门面、状态、修订、历史、脱敏
│   ├── registry/                      协议、二进制和资源能力注册表
│   ├── adapters/                      Snell、AnyTLS、Hysteria2 内容构建器
│   ├── resources/                     binary、certificate、firewall、systemd
│   ├── transaction/                   plan、snapshot、apply、verify、rollback
│   ├── orchestrators/                 deploy、config_apply、update、uninstall
│   ├── export/                        Surge、二维码与导出状态
│   └── interface/                     菜单、交互流程、开发 CLI
│
├── tools/
│   └── config_tool.py                 受控 Python YAML 辅助工具
│
├── templates/                         仅保存静态模板或模板骨架
│   ├── runtime/
│   └── systemd/
│
├── tests/
│   ├── unit/
│   ├── node/
│   ├── integration/
│   ├── fault/
│   └── smoke/
│
├── docs/                              正式开发后复制或引用的基线文档
└── packaging/                         发布包、校验与解包回归资料
```

这是推荐职责目录，而不是要求一开始创建所有空文件。只有具有稳定边界、独立测试价值或会被多处复用的模块，才应独立存在。

---

## 三、模块责任与禁止事项

### 3.1 Core：公共基础能力

**负责**

```text
路径与常量；
日志；
终端渲染；
输入读取；
编号菜单校验；
端口、IP、域名、范围、密码等通用校验；
明确确认；
全局操作锁；
中断信号处理；
临时目录与权限辅助。
```

**不得负责**

```text
读取或写入具体协议配置；
决定协议部署顺序；
执行 systemd、证书、防火墙或下载业务；
标记操作已成功。
```

### 3.2 Config & State：事实来源、状态与修订版

**负责**

```text
调用 Python 配置工具读取和写入主 YAML；
schema_version 与 config_revision；
desired / applied / observed 的持久化；
敏感字段脱敏；
历史摘要；
最近 5 个成功修订版的保存、列出和恢复准备；
配置文件、快照与备份的权限控制。
```

**不得负责**

```text
自行决定哪些服务要重启；
自行生成协议运行配置；
直接调用 systemd、防火墙或证书客户端；
在健康检查前把 applied 标记为成功。
```

**责任边界**

配置层只保证数据正确、可恢复与安全；“何时提交配置”和“是否应恢复旧状态”由事务层与编排器决定。

### 3.3 Registry：协议与资源能力描述

**负责**

```text
协议的静态能力描述；
协议参数字段、默认值、类型、是否必填与校验规则；
网络类型与端口资源声明；
二进制资源、共享关系和版本兼容规则；
协议所需证书、服务名、runtime 路径与健康检查方式；
对应适配器构建器的映射。
```

**不得负责**

```text
保存用户实际输入的端口、密码、PSK、域名或带宽；
读取菜单输入；
写主配置；
执行系统命令；
隐藏协议特有差异以制造错误的“完全通用”。
```

注册表描述的是“协议有什么能力”，主配置保存的是“这台 VPS 实际采用了什么值”。

### 3.4 Protocol Adapter：协议内容构建器

**负责**

```text
对某协议参数进行协议级校验；
根据 desired 配置和资源路径构建 runtime 内容；
构建 systemd unit 描述；
构建 Surge 条目；
生成协议相关 config patch；
声明该协议需要的网络、证书、二进制和健康检查资源。
```

**不得负责**

```text
读取终端输入；
直接写文件；
直接调用 systemctl、acme.sh、下载工具或防火墙；
修改主配置；
决定是否部署其他协议；
决定事务提交或回滚。
```

适配器返回“内容与资源描述”，通用写入与资源模块负责实际落盘和执行。

### 3.5 Resource Primitive：外部资源原语

资源原语只执行单一类外部动作，并返回结构化结果。

| 资源模块 | 负责 | 不得负责 |
|---|---|---|
| Binary | 版本候选、下载、完整性校验、解压、试运行、安装、替换、恢复 | 决定是否更新、批量更新策略 |
| Certificate | DNS/TCP 80 检查、候选证书申请、安装、状态读取、续期 timer 原语 | 决定协议组合、提交整体成功状态 |
| Firewall | 检测支持工具、TCP/UDP/range 规则计划与应用 | 决定协议需求、删除策略之外的业务判断 |
| Systemd | unit 原子写入、daemon-reload、start/stop/restart、enable/disable、日志与状态读取 | 决定哪些服务受某次业务变更影响 |
| Runtime writer | 对适配器内容执行原子写入、权限设置、文件恢复 | 自行生成协议内容 |

所有资源原语都必须支持明确的 dry-run 输出，且 dry-run 结果不得被写成真实成功状态。

### 3.6 Transaction：高影响操作的执行骨架

**负责**

```text
建立操作计划；
校验计划是否完整；
创建临时快照；
按步骤执行资源动作；
记录已完成步骤；
请求健康验证；
提交 applied 状态和成功修订版；
失败时按逆序调用恢复动作；
在恢复失败时标记状态不确定并生成修复上下文。
```

**不得负责**

```text
自行解释协议业务；
自行从菜单收集用户输入；
决定某个字段变更应影响什么协议；
绕过编排器直接构造业务计划。
```

### 3.7 Orchestrator：业务规则的唯一执行者

**负责**

```text
根据需求规则构建部署、配置应用、更新和卸载计划；
计算协议、证书、端口、防火墙、服务和 Surge 导出的影响范围；
调用适配器和资源原语；
将业务计划交给事务层；
汇总结果并决定用户可见的操作状态。
```

**不得负责**

```text
直接解析菜单输入；
把底层命令细节复制到多个编排流程；
跳过事务直接修改活动资源；
由某个协议特例改变全局产品规则。
```

v1 必须至少有：

```text
deploy orchestrator
config-apply orchestrator
update orchestrator
uninstall orchestrator
certificate workflow orchestrator
revision-restore orchestrator
```

### 3.8 Interface：正式菜单与开发 CLI

**负责**

```text
展示菜单与当前状态；
收集用户意图；
调用统一校验；
展示计划、预览、确认和结果；
调用编排器。
```

**不得负责**

```text
直接修改主配置；
直接生成运行配置；
执行系统资源命令；
在菜单中实现部署、更新或回滚逻辑。
```

正式菜单最后接入；但从底座阶段起应存在开发 CLI 或测试 harness，用于运行 dry-run、节点测试和编排测试。

---

## 四、关键数据与状态模型

### 4.1 主配置的职责

主 YAML 保存三类持久化信息：

```text
desired：用户希望系统采用的配置；
applied：最近一次成功应用的配置版本与资源版本；
observed：最近检查到的服务与资源摘要。
```

建议的顶层语义为：

```text
meta                 schema、创建/修改时间、脚本版本
desired              用户意图：global、protocols、certificate、surge 偏好
applied              已成功提交的修订与资源版本摘要
observed             最近系统观察结果
history              有限历史摘要
```

旧草案中按 `global`、`protocols`、`certificate`、`services`、`surge` 分组的语义应保留；技术设计阶段将它们置于上述三类状态之下，避免 `enabled` 同时表达期望、已应用与实际运行。

### 4.2 修订版与事务快照的区别

| 类型 | 创建时机 | 保留数量 | 用途 |
|---|---|---|---|
| 事务快照 | 高影响操作开始前 | 仅操作期间 | 失败时恢复到操作前状态 |
| 成功修订版 | 高影响操作健康验证并提交后 | 最近 5 个 | 用户选择历史配置恢复 |
| 历史摘要 | 操作结束后 | 有限条数 | 告诉用户发生过什么，不直接恢复 |

成功修订版必须包含足够恢复上下文：配置、受影响派生文件、资源版本、证书和防火墙状态摘要。恢复历史修订本身也是新的高影响操作，必须经过事务层。

### 4.3 成功状态的提交点

以下情况不得写入 `applied.success`：

```text
二进制尚未下载或未校验；
runtime 或 unit 尚未原子安装；
防火墙尚未按计划处理；
服务尚未通过健康验证；
事务尚未完成必要恢复或结果记录。
```

服务端已健康验证、但 Surge 导出失败时：

```text
applied：服务端变更成功；
surge export：单独标记失败；
config_dirty：不因单纯导出失败而必然置为 true；
结果：明确提示客户端尚未同步。
```

---

## 五、协议注册表与适配器模型

### 5.1 注册表最小字段

每个协议注册项至少应能表达：

| 字段类别 | 示例 |
|---|---|
| 身份 | `id=snell`、显示名称、版本代际 |
| 网络 | `tcp`、`udp`、`udp-range`；默认端口与冲突资源 |
| TLS | 是否需要证书、引用哪一种证书策略 |
| 二进制 | 资源 ID、共享关系、最低/兼容版本规则 |
| 服务 | unit 名称、runtime 路径、启动命令描述 |
| 参数 | 类型、默认值、必填性、敏感性、校验与依赖规则 |
| 构建器 | runtime、unit、Surge、config patch、健康检查构建器 |
| 更新 | 版本检测方式、兼容检查策略 |

### 5.2 v1 协议差异

| 协议 | TLS | 网络资源 | 定制参数重点 | 特殊限制 |
|---|---|---|---|---|
| Snell v6 | 否 | TCP 单端口 | PSK、客户端地址类型、mode | v1 仅 `mode=default` |
| AnyTLS | 是，共享证书 | TCP 单端口 | 密码、TFO、reuse | 使用共享 sing-box 资源时需检查兼容性 |
| Hysteria2 | 是，共享证书 | UDP 单端口 + 可选 UDP 范围 | 密码、跳跃、Gecko、带宽 | 主端口不得落入跳跃范围 |

### 5.3 协议参数的所有权

```text
注册表：参数存在性、类型、默认值、敏感性和校验规则。
desired 配置：用户实际填写或选择的值及其来源。
适配器：将实际值转为 runtime/unit/Surge 内容。
编排器：判断一次变更会影响哪些资源和服务。
```

这样既允许每个协议拥有定制参数，也避免把协议特例散落到菜单、防火墙和更新逻辑中。

---

## 六、资源模块的技术承接

### 6.1 Binary：版本、完整性与缓存

二进制模块必须提供以下原语：

```text
列出当前 Debian 13 amd64 可用的正式版本；
筛选最新 3 个具有明确版本号、发布日期和完整性信息的候选版本；
下载到临时目录；
校验 checksum 或签名；
按压缩格式解压并确认可执行文件；
运行版本检测；
安装、备份、替换与恢复；
记录二进制版本、来源和校验信息。
```

候选版本由用户明确选择。共享二进制必须先计算所有消费者的兼容范围，再展示候选版本。

### 6.2 Certificate：候选资源与活动资源分离

证书模块不直接将新证书覆盖活动证书。它应支持：

```text
预检查 DNS、当前 IP、TCP 80 和本机防火墙风险；
申请候选证书；
检查候选证书有效性；
备份当前活动证书；
原子安装候选证书；
让编排器重载相关服务并请求健康验证；
在失败时恢复本机旧活动证书。
```

外部机构签发的新证书不是本机事务可撤销对象；事务承诺的是不在未验证成功前破坏当前活动证书和服务。

### 6.3 Firewall：资源计划而非协议猜测

防火墙模块输入必须是明确的网络资源计划，例如：

```text
allow tcp 443
allow udp 8443
allow udp 20000-50000
allow tcp 80 temporarily for certificate challenge
```

它不应从协议名称猜测规则，也不应统一放行 TCP 和 UDP。

v1 的 auto 模式优先实现 ufw 与 nftables；其他或无法判断的环境返回可读的 manual 指引。

### 6.4 Systemd：操作原语与观察原语分离

systemd 模块提供：

```text
unit 原子安装/恢复；
daemon-reload；
start/stop/restart；
enable/disable；
is-active、is-enabled；
有限行数 journal 摘要；
服务状态结构化观察。
```

是否要启用、重启或删除哪个 unit，始终由编排器决定。

### 6.5 Runtime 与 Surge：内容构建和原子落盘分离

```text
协议适配器：构建内容。
writer：临时写入、权限设置、校验和原子替换。
Surge 导出器：收集当前已应用协议的条目，构建完整输出和二维码。
```

导出器不应回读 runtime 文件来恢复主配置，也不得在导出失败时回滚服务端已验证成功的变更。

---

## 七、事务、验证与恢复模型

### 7.1 操作计划

每个高影响操作必须先构建可显示、可验证的计划。计划至少含：

```text
操作 ID 与类型；
目标协议与资源；
配置变更 patch；
受影响 runtime/unit/二进制/证书/防火墙/服务/Surge 资源；
执行步骤与每步恢复动作；
健康验证步骤；
不回滚但需要记录的失败；
最终提交条件。
```

### 7.2 事务步骤

```text
1. acquire lock
2. validate plan and dependencies
3. render preview
4. receive explicit confirmation
5. create transaction snapshot
6. mark operation pending
7. execute resource steps
8. perform health verification
9. commit desired/applied/observed state
10. export Surge when required
11. record history and successful revision
12. release lock
```

若任一步失败：

```text
stop subsequent steps
→ execute required restore actions in reverse order
→ verify restored service state
→ write rollback-success, failed or dirty result
→ record actionable repair guidance
→ release lock
```

### 7.3 适用操作

| 操作 | 必须使用事务 | 关键验证 |
|---|---|---|
| 部署 | 是 | 二进制、文件、网络、服务、Surge |
| 配置应用 | 是 | 受影响资源与服务 |
| 更新 | 是 | 新版本、服务健康、旧版本恢复 |
| 卸载 | 是 | 删除结果、依赖保护、剩余服务 |
| 证书申请/切换/续期 | 是 | 证书有效、TLS 服务健康 |
| 历史修订恢复 | 是 | 目标资源恢复与健康 |
| 查看状态/日志 | 否 | 只读结果 |
| 启动/停止 | 简化确认流程 | 目标 unit 结果 |

### 7.4 健康检查责任

健康检查由“协议适配器提供预期 + systemd/网络资源实际观察”共同组成：

```text
适配器：声明预期 unit、二进制、TCP/UDP 监听与配置检查方式。
systemd：确认 unit active、读取有限日志。
网络观察：确认正确端口和传输方式监听。
编排器：判断所有条件是否足以提交成功。
```

最终 iOS Surge 连接属于发布或重要功能变更后的真实验收，不应被伪装为每次局部单元测试的结果。

---

## 八、主要编排器的定位

### 8.1 Deploy orchestrator

输入：协议选择与部署草案。  
负责：协议组合校验、TLS 判断、证书预检、端口冲突、二进制版本选择、安装计划、部署事务、健康验证与导出。  
不得：由菜单直接拼接命令；提前写入成功 applied 状态。

### 8.2 Config-apply orchestrator

输入：单次字段变更 patch。  
负责：读取旧值、校验、影响分析、预览、事务应用、受影响资源重建、恢复和修订记录。  
不得：提供无上下文的“应用全部未提交变更”入口。

### 8.3 Update orchestrator

输入：目标服务与用户明确选择的版本。  
负责：兼容候选筛选、备份、临时安装、版本检查、服务验证、批量停止策略、恢复与版本状态提交。  
不得：无条件安装上游最新版本或批量并发替换。

### 8.4 Uninstall orchestrator

输入：目标协议或全部已安装协议。  
负责：影响预览、依赖判断、停止/禁用/删除、共享资源保护、状态/Surge 更新和准确结果。  
不得：卸载 Snell 时触碰证书；卸载最后 TLS 协议时自动删除证书或 timer。

### 8.5 Certificate workflow orchestrator

输入：申请、续期、启用/停用 timer、清理等用户意图。  
负责：预检查、候选证书流程、相关 TLS 服务影响、事务、状态与结果。  
不得：绕开共享证书规则或在失败时删除旧活动证书。

### 8.6 Revision-restore orchestrator

输入：用户选择的历史成功修订号。  
负责：显示恢复影响、当前快照、目标恢复、资源版本处理、健康验证、失败恢复与新修订提交。  
不得：直接复制旧 YAML 后宣称恢复完成。

---

## 九、权限、调用与安全规则

### 9.1 主配置写权限

只有 Config & State 模块可以实际写主配置；编排器只能请求其在事务提交点写入 patch 或状态。

### 9.2 系统资源执行权限

只有 Resource Primitive 模块可以调用：

```text
systemctl / journalctl
acme.sh
下载与解压工具
ufw / nftables
文件安装与权限调整
```

### 9.3 调用禁止清单

```text
菜单层不得直接调用 systemctl、下载、证书或防火墙。
协议适配器不得写主配置或直接执行系统命令。
资源原语不得决定协议组合、服务影响或最终业务结果。
Python 配置工具不得执行系统部署动作。
事务层不得自行发明协议业务规则。
编排器不得以绕过快照和健康验证的方式修改活动资源。
```

### 9.4 敏感数据规则

```text
主配置、修订快照和含密钥的派生文件至少为 600。
日志、普通预览、状态与错误页面默认脱敏。
仅在用户明确请求 Surge 客户端配置时输出所需完整敏感参数。
临时文件、下载目录和中间输出在操作结束后按结果安全清理或归档。
```

---

## 十、测试与真实验证责任

### 10.1 测试层次

| 层次 | 主要对象 | 示例 |
|---|---|---|
| Unit | 校验器、格式化、版本筛选、状态转换 | 端口、PSK、版本选择 |
| Node | 单个模块的输入输出 | YAML 工具、Snell 适配器、firewall plan |
| Integration | 多节点组合 | 计划 + 适配器 + writer + 模拟 systemd |
| Fault | 失败与恢复 | 下载失败、服务失败、回滚失败 |
| Smoke | CLI/菜单主要路径 | 多选、确认、取消、状态查看 |
| Debian 13 real | 真实资源与 Surge | 证书、监听、服务、iOS 连接 |

### 10.2 需求与测试映射

《05. Surge 二次开发需求追踪矩阵》中的每个 P0 需求都必须在节点契约中补全：

```text
对应模块；
自动化测试文件或场景；
故障注入点；
是否需要 Debian 13 真实验证；
验证记录位置。
```

### 10.3 真实 VPS 使用原则

```text
不要求为每次候选版本重建 VPS；
局部节点优先使用 mock、dry-run 和隔离目录；
涉及真实系统资源的变化，先做预览和快照；
只在需要确认真实协议、证书、监听或 Surge 连接时使用 VPS；
每次真实验证记录已测范围与未测范围。
```

---

## 十一、内部开发顺序与外部里程碑

### 11.1 内部开发依赖顺序

该顺序表达依赖，不等同于对外线性 Stage：

```text
需求追踪与技术决策
→ Core + Python YAML 工具
→ Config / State / Revision / Lock
→ Registry + Transaction
→ Protocol adapters 与资源原语
→ Deploy / Config apply / Update / Uninstall 编排
→ Surge 完整导出
→ 正式菜单
→ 故障注入与 Debian 13 真实验证
```

Snell 适配器可先用于验证最短服务链路；但证书、AnyTLS、Hysteria2、更新和卸载不应以复制 Snell 流程的方式各自重建一套事务。

### 11.2 建议里程碑

| 里程碑 | 交付内容 | 最低通过条件 |
|---|---|---|
| M1 技术底座 | 配置工具、状态、修订、锁、测试 harness | 配置原子写入与恢复测试通过 |
| M2 协议与资源节点 | 注册表、三协议适配器、binary/cert/firewall/systemd 原语 | 各节点独立测试通过 |
| M3 业务编排 | 部署、修改、更新、卸载、证书与恢复流程 | 关键故障注入通过 |
| M4 用户交互 | 正式菜单、预览、确认、结果、日志 | Smoke 测试覆盖主要路径 |
| M5 候选验收 | 完整包、已知限制、Debian 13 真实验证 | P0 达到 verified 或明确延期 |

对外交付每个里程碑的完整快照；内部允许不同独立节点并行推进，只要其契约和依赖已明确。

---

## 十二、首批节点契约清单

以下节点应在开始代码前优先写成独立契约：

| 节点 | 原因 | 关联 P0 |
|---|---|---|
| Python YAML configuration tool | 主配置可靠性的唯一入口 | CONFIG-001, STATE-001 |
| Config / state / revision manager | 三类状态、5 次修订和权限边界 | CONFIG-001, STATE-001 |
| Operation transaction + rollback | 所有高影响操作共用 | TRANS-001, UPDATE-001 |
| Protocol registry | 协议参数、网络与资源差异来源 | Snell, AnyTLS, HY2, BIN, FW |
| Binary resource | 三个具体版本、校验和恢复 | BIN-001, UPDATE-001 |
| Systemd service primitive | unit、状态、日志与健康观察 | SVC-001, TRANS-001 |
| Firewall resource | TCP/UDP/range 正确性 | FW-001 |
| Snell adapter | 最短协议闭环验证 | SNELL-001 |
| Certificate resource | 共享 TLS 的高风险外部依赖 | CERT-001, AnyTLS, HY2 |

AnyTLS、Hysteria2、Surge、部署、配置应用、更新、卸载等节点可在上述公共边界完成后逐步建立契约。

---

## 十三、进入代码开发前的检查

```text
[x] 产品需求基线已存在。
[x] 技术决策已冻结。
[x] P0/P1 需求已编号、可追踪并具备验收方式。
[x] 模块、调用方向、资源所有权与事务责任已定义。
[x] Bash 与 Python 边界已定义。
[x] Debian 13 真实验证策略已定义。
[ ] 首批节点契约已完成。
[ ] 代码工作区、测试入口与开发规范已建立。
```

完成首批节点契约后，才进入 Codex 本地开发与调试阶段。开发任务应始终引用：需求 ID、节点契约、允许修改范围和必须验证的测试。
