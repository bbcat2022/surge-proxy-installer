
> 本文档记录 Surge 一键安装脚本二次开发前已经确认的技术决策。它是《开发模块及定位》、节点契约、测试计划和实现任务的上游约束，用于避免开发过程中重新从长对话中猜测或搜索结论。

**状态**：已冻结，后续变化须通过“技术契约变更”或“需求变更”记录。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 一、文档定位与优先级

本文档只记录已经确认的技术取舍，不替代产品需求基线。

文档关系如下：

```text
产品需求基线
→ 本技术决策基线
→ 《开发模块及定位》
→ 节点契约与实现
→ 测试、真实验收与交付
```

当产品需求文档与技术设计存在冲突时，应优先确认是否发生了需求变更；技术实现不得默默改变已经冻结的用户可见行为。

当本文档与早期讨论、旧阶段包或旧技术建议存在冲突时，以本文档为准。

---

## 二、开发与交付模型

### DEC-001：采用双轨制，而不是线性 Stage 开发

**结论**

```text
内部开发：按模块树、节点契约和依赖关系推进。
外部交付：按里程碑提供完整快照、验收结论和已知限制。
```

Snell、AnyTLS、Hysteria2 的实现顺序仅是风险由低到高的建议验证顺序，不构成必须等待前一条完整阶段结束的线性流水线。

**实现约束**

- 一个开发任务应聚焦一个节点或一个明确的编排流程。
- 节点完成后先运行节点测试和直接相关集成测试。
- 完整项目快照仅在里程碑或候选验收时提供。
- 不得为了完成当前节点而绕过模块边界修改无关模块。

### DEC-002：v1 完成条件由需求与验收门槛决定

**结论**

v1 是否完成不以“所有预设阶段已经打钩”为判断，而以发布门槛判断：

```text
范围内的 P0 需求均已实现；
每项 P0 需求均有相应测试；
高风险操作具有事务、验证和失败处理；
在目标 Debian 13 VPS 上完成真实使用验证；
iOS Surge 完成目标协议连接验证；
未完成内容明确标为 deferred 或 out-of-scope。
```

---

## 三、目标运行环境

### DEC-003：v1 的唯一已验证支持环境为 Debian 13 amd64

**结论**

```text
操作系统：Debian 13
CPU 架构：amd64
初始化系统：systemd
包管理器：apt
Shell：Bash 5+
辅助运行时：Python 3
客户端：iOS Surge
部署对象：需求方独享 VPS
```

**支持边界**

- v1 仅承诺并记录 Debian 13 amd64 的真实验证结果。
- 不要求为 Ubuntu 22.04、Debian 12 或其他系统反复重建 VPS 进行兼容性验收。
- 实现应避免仅凭发行版版本号拒绝更高版本的 Ubuntu 或 Debian；应优先进行能力检测。
- 未经真实验证的系统不得在发布说明中写为“已支持”或“已验证”。

**替代的早期建议**

早期“Ubuntu 22.04/24.04 与 Debian 12 的首发验证矩阵”不再是 v1 发布门槛；它们仅保留为未来可能的兼容方向。

---

## 四、语言与配置处理

### DEC-004：采用 Bash 主体与受控 Python 辅助工具

**结论**

```text
Bash：负责菜单、用户交互、业务编排、系统操作和结果展示。
Python：仅负责 YAML 的结构化处理与配置文件安全写入。
```

脚本在准备环境时可安装或检查：

```text
python3
python3-yaml
```

**Python 允许的职责**

```text
读取 YAML；
校验 YAML 结构；
按字段路径读取、修改或合并配置 patch；
生成临时 YAML；
校验临时 YAML；
执行配置文件原子替换；
生成适合 Bash 消费的结构化结果。
```

**Python 不得承担的职责**

```text
调用 systemctl；
下载或替换协议二进制；
修改防火墙；
申请、切换或清理证书；
决定部署、更新或卸载顺序；
直接提交“部署成功”或“服务健康”的业务状态。
```

### DEC-005：YAML 是唯一主配置格式，必须结构化读写

**结论**

```text
主配置格式：YAML。
主配置是用户意图和已提交应用状态的唯一事实来源。
配置读写统一经过 Python 配置辅助工具。
禁止由 Bash 通过文本替换直接修改 config.yaml。
```

**写入规则**

```text
读取
→ schema 校验
→ 结构化修改
→ 写入临时文件
→ 校验临时文件
→ 原子替换正式文件
→ 保留可恢复备份。
```

运行配置、systemd unit、Surge 导出文件均为派生资源，不得反向作为修改主配置的依据。

---

## 五、配置修订、状态与回滚

### DEC-006：使用 desired / applied / observed 三类状态

**结论**

```text
desired：用户希望系统采用的配置。
applied：最近一次已成功应用的配置版本、结果和资源版本。
observed：最近一次从真实系统检测到的状态摘要。
```

建议语义如下：

| 分类 | 保存内容示例 | 不应表示 |
|---|---|---|
| desired | 域名、端口、密码、开机自启、防火墙模式 | 服务已经实际运行 |
| applied | 已应用修订号、应用时间、二进制版本、结果摘要 | 最新实时系统状态 |
| observed | 服务是否 active、是否已自启、端口监听、检查时间 | 用户未来希望采用的设置 |

主配置必须包含：

```text
schema_version
config_revision
```

详细日志不写入主 YAML；只在 YAML 中保存必要摘要，详细过程写入日志文件。

### DEC-007：保留最近 5 个成功应用的配置修订版

**结论**

每次高影响操作成功后，形成一个“成功应用的配置修订版”。系统保留最近 5 个修订版，供用户选择回滚。

每个修订版至少包含：

```text
修订号；
完成时间；
操作类型；
变更摘要；
主配置快照；
受影响的 runtime 配置快照；
受影响的 systemd unit 快照；
Surge 导出快照；
二进制版本与来源记录；
证书与防火墙的状态摘要。
```

**保留与权限规则**

- 仅保留最近 5 个成功修订版；第 6 个成功修订版生成后清理最旧一份。
- 进行中的操作使用独立临时快照，不占用 5 个长期修订位。
- 包含密钥或敏感配置的快照、备份和派生文件必须使用严格权限，至少为 `600`。
- 修订版必须记录是否可直接恢复，或是否需要重新下载特定二进制。

**回滚规则**

选择一个历史修订版后，必须运行统一事务：

```text
展示恢复影响
→ 用户明确确认
→ 创建当前临时快照
→ 恢复目标配置与派生资源
→ 恢复或重新取得目标二进制版本
→ 应用必要的服务和防火墙变更
→ 健康检查
→ 成功后形成新的当前修订版
→ 失败时恢复操作前状态。
```

证书的外部签发不可撤销；回滚的目标是恢复本机此前活动且可用的证书和相关服务状态，而非撤销已被证书机构签发的新证书。

---

## 六、协议注册表与协议差异

### DEC-008：以协议注册表统一描述协议能力和定制参数

**结论**

每个协议都必须由注册表描述其通用能力、可用参数、默认值和校验规则，供交互、校验、编排、runtime、systemd、Surge 导出和测试共同使用。

注册表至少包含：

```text
协议标识与显示名称；
网络类型：tcp / udp / udp-range；
默认端口；
是否需要 TLS 证书；
二进制资源标识；
服务名与 runtime 路径；
支持的定制参数；
参数类型、默认值、是否必填与校验规则；
健康检查方式；
runtime、unit、Surge 条目的构建器标识。
```

**边界**

```text
注册表：描述“协议有哪些能力和参数规则”。
主配置：保存“用户在这台 VPS 上实际选择了什么值”。
协议适配器：根据注册表与实际配置生成协议内容。
```

不得将用户的密码、PSK、实际端口、域名等实例数据硬编码进注册表。

---

## 七、统一事务、健康验证与操作保护

### DEC-009：高影响操作共用事务与回滚框架

**适用范围**

```text
部署服务；
应用配置修改；
更新协议二进制；
卸载服务；
申请证书；
切换证书；
证书续期。
```

普通查看状态、查看日志、启动、停止等低影响操作不要求创建完整快照，但仍应明确报告结果。

**统一流程**

```text
收集意图
→ 构建操作计划
→ 校验计划与依赖
→ 展示影响
→ 用户明确确认
→ 创建快照
→ 标记执行中
→ 执行步骤
→ 健康验证
→ 提交最终状态
→ 导出 Surge
→ 写入历史与结果。
```

**失败流程**

```text
停止后续步骤
→ 回滚已完成且必须恢复的步骤
→ 验证恢复结果
→ 标记成功回滚、失败或状态不确定
→ 记录错误与人工修复建议。
```

Surge 导出失败不得回滚已经健康验证成功的服务端变更，但必须单独记录并提示客户端配置未同步。

### DEC-010：证书更新与续期必须进入同一事务框架

**结论**

证书申请、切换和续期不是独立的“尽力而为”操作，必须使用 DEC-009 的事务框架。

证书续期的最小流程：

```text
检查续期条件
→ 申请候选证书
→ 校验候选证书
→ 备份当前活动证书与状态
→ 原子切换本机活动证书
→ 重启或重载受影响 TLS 服务
→ 健康检查
→ 提交证书状态与历史。
```

如果服务无法恢复：

```text
恢复旧活动证书
→ 恢复相关服务
→ 验证恢复结果
→ 记录失败或状态不确定。
```

### DEC-011：服务健康检查必须证明服务可持续运行

任何部署、更新或配置应用成功前，至少验证：

```text
对应 systemd unit 处于 active；
对应端口按正确 TCP / UDP 方式监听；
runtime 已通过协议自身的配置或启动验证；
最近日志不存在明确启动失败；
服务实际加载的是预期二进制版本。
```

`systemctl restart` 返回成功不是健康验证的充分条件。

使用 iOS Surge 的真实连接验证属于目标 VPS 的最终验收层，不替代上述自动化健康检查。

### DEC-012：提供锁、中断处理与重复执行保护

高影响操作必须具备：

```text
全局排他锁：同一时刻只允许一个高影响操作；
中断处理：收到 Ctrl+C 或异常终止时记录步骤、尝试恢复或标记待修复；
重复执行保护：已安装协议再次部署时展示覆盖影响并要求确认；
操作编号：可从日志、历史与错误摘要定位失败位置。
```

---

## 八、二进制资源与版本选择

### DEC-013：二进制使用版本清单与官方完整性验证

每个二进制资源必须定义：

```text
官方发布来源；
支持的 CPU 架构映射；
压缩格式与解压规则；
可执行文件名与安装路径；
版本检测命令；
可信校验和或签名来源；
与协议的兼容规则；
替换、备份和恢复策略。
```

脚本不得无条件下载“最新版本”后直接替换正在使用的二进制。

### DEC-014：安装与更新时展示最新三个具体兼容版本

**结论**

对每个需要安装或更新的二进制，脚本从官方发布来源筛选当前环境兼容的正式版本，并明确展示最新 3 个具体版本号。

示例：

```text
请选择 Snell Server 版本：

1. v5.0.1（最新稳定版，发布于 YYYY-MM-DD）
2. v5.0.0（上一稳定版，发布于 YYYY-MM-DD）
3. v4.9.8（上上稳定版，发布于 YYYY-MM-DD）
4. 返回上一级

当前已安装：v5.0.0
```

展示的候选版本必须同时满足：

```text
适配当前 CPU 架构；
属于上游正式 release；
与当前已启用协议兼容；
可获得可信完整性校验信息。
```

如果官方来源没有可用的可信校验信息，脚本必须拒绝安装或明确提示风险；不得静默跳过验证。

共享二进制（例如同时被多个已启用能力使用的资源）更新时，必须在版本选择前展示影响，并只允许选择与所有相关能力兼容的版本。

### DEC-015：v1 不提供脚本本体在线自更新

v1 只支持协议二进制的受控更新。脚本本体通过完整发布包进行手动升级，以降低自更新损坏核心编排逻辑的风险。

---

## 九、证书与防火墙

### DEC-016：v1 只支持一个主域名与一张共享 TLS 证书

**结论**

```text
AnyTLS 与 Hysteria2 共用同一个主域名和同一张 TLS 证书。
Snell v6 不使用 TLS 证书。
v1 不支持同一 VPS 上多个 TLS 域名或每协议独立证书。
```

证书策略继承既有产品基线：Let’s Encrypt、acme.sh、HTTP-01 standalone。

当 TCP 80 被常见 Web 服务占用时，脚本必须展示影响并要求用户明确确认后才临时停止；申请完成后应恢复原服务状态。

### DEC-017：防火墙采用 manual / auto 模式，并按传输类型处理

**manual 模式**

```text
脚本不修改本机防火墙；
准确展示需要放行的 TCP、UDP 或 UDP 范围；
提示云安全组规则仍需用户处理。
```

**auto 模式**

```text
仅对已识别且已支持的本机防火墙工具添加规则；
v1 优先支持 ufw 与 nftables；
不自动删除旧规则；
始终提示云安全组仍需单独确认。
```

检测到未支持的防火墙环境时，必须明确降级为 manual 模式或终止本次自动操作，不得执行未经验证的规则。

防火墙接口必须区分：

```text
TCP 单端口；
UDP 单端口；
UDP 端口范围。
```

---

## 十、真实使用验证与版本说明

### DEC-018：真实验证只使用需求方的 Debian 13 VPS

**结论**

本脚本仅供需求方个人使用，真实验证以需求方独享的 Debian 13 amd64 VPS 为唯一目标环境，不要求为每个候选版本反复重建或重置 VPS。

**验证策略**

```text
节点开发：本地隔离测试、mock、dry-run、故障注入。
候选版本：在现有 Debian 13 VPS 上执行尽量低风险的目标测试。
真实部署：仅在需要验证的功能变更时执行，不要求每次版本都重建环境。
发布记录：明确写出本次实际测试内容与未测试内容。
```

v1 的真实验收至少应包含：

```text
官方二进制下载、架构选择、完整性校验、解压和版本检查；
Snell、AnyTLS、Hysteria2 的真实部署与健康检查；
共享证书申请、安装、续期和相关服务恢复；
TCP、UDP、UDP 范围的防火墙行为；
部署、更新或配置修改中的至少一条关键失败路径；
使用 iOS Surge 完成目标协议的实际连接验证；
打包、解包和安装后的基本 smoke 测试。
```

---

## 十一、后续文档承接要求

《开发模块及定位》必须承接并引用本文件，至少明确：

```text
各模块如何遵守 Bash / Python 边界；
配置辅助工具与主配置的调用方式；
协议注册表的实现位置、字段与调用关系；
desired / applied / observed 的具体 schema；
配置修订、快照、恢复与保留策略；
事务、回滚、健康检查和锁的模块归属；
二进制版本清单、候选版本筛选与缓存策略；
防火墙、证书和 systemd 的资源原语边界；
目标 Debian 13 VPS 的测试与发布记录方式。
```

任何实现若无法满足本文某项决策，必须先提出技术契约变更，不得自行弱化或绕过。
