
> 本文档用于将 Surge 一键安装脚本从“开发前文档阶段”正式交接到 Codex 本地开发与调试阶段。它规定 Codex 每次任务必须读取什么、可以修改什么、如何验证、何时允许触碰真实 Debian 13 VPS，以及如何把开发结果写回追踪矩阵。

**状态**：开发交接基线。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 一、交接结论

开发前文档已经完成以下工作：

```text
产品需求已被整理为可追踪需求；
关键技术决策已冻结；
模块、调用方向、资源所有权和事务责任已定义；
首批高风险节点已具备契约；
测试、故障注入和 Debian 13 验收策略已定义。
```

从本清单生效后，可以切换到 Codex 本地开发。开发应从首批节点 N1 开始，不直接编写完整菜单、协议部署流程或真实 VPS 安装脚本。

```text
第一个实现节点：N1 Python YAML 配置工具。
第一个目标：建立可靠的主配置读写入口。
第一个真实 VPS 原则：不在 N1 阶段操作代理服务、systemd、防火墙、证书或活动二进制。
```

---

## 二、Codex 必读文档

每次开始新的本地开发任务前，Codex 必须先读取与当前任务相关的基线文档。

### 2.1 始终必读

```text
surge-technical-decision-baseline.md
surge-requirements-traceability-matrix.md
6. 开发模块及定位.md
7. Surge 二次开发节点契约总览.md
8. 测试、故障注入与 Debian 13 验收计划.md
AGENTS.md
```

### 2.2 按任务补充阅读

| 任务类型 | 必读补充材料 |
|---|---|
| 菜单、输入、预览、确认 | 最终菜单树、交互问答流程稿 |
| 主配置、状态、修订、脱敏 | 配置文件结构草案、技术决策基线 |
| 部署、修改、更新、卸载 | 联动规则与异常处理清单、需求追踪矩阵 |
| 协议实现 | 对应协议需求、注册表契约、Surge 输出规则 |
| 证书 | 证书流程、联动规则、证书节点契约 |
| 真实 VPS 验收 | 测试、故障注入与 Debian 13 验收计划 |

### 2.3 文档优先级

当旧阶段包、早期草稿、对话摘要和当前基线存在差异时：

```text
产品需求基线
→ 技术决策基线
→ 需求追踪矩阵
→ 开发模块及定位
→ 节点契约
→ 早期实现或阶段包
```

若仍无法判断，不得自行猜测；应记录待决问题或提出契约变更。

---

## 三、工作区规则

### 3.1 文档与代码的边界

```text
sources/ 下的同步项目资料为只读参考，不编辑、不移动、不删除。
开发产生的代码、测试、文档和打包资料应位于新的实际项目目录中。
开发前基线文档应复制或引用到项目 docs/ 目录，保留版本来源。
```

### 3.2 初始项目结构

进入实际开发时，先创建轻模块化 Bash 项目骨架：

```text
proxy-installer/
├── bin/
├── lib/
│   ├── core/
│   ├── config/
│   ├── registry/
│   ├── adapters/
│   ├── resources/
│   ├── transaction/
│   ├── orchestrators/
│   ├── export/
│   └── interface/
├── tools/
├── templates/
├── tests/
├── docs/
├── packaging/
├── README.md
└── AGENTS.md
```

不要求第一天创建所有空目录或文件。只创建当前节点实际需要的最小结构，避免空壳模块和过度工程化。

### 3.3 项目级 AGENTS.md 应包含

实际项目的 `AGENTS.md` 应将以下规则固化为 Codex 的本地开发约束：

```text
主配置只通过 Python YAML 工具读写；
菜单层不得直接修改配置或调用系统命令；
协议适配器不得直接写文件或调用 systemd；
高影响操作必须通过事务层；
测试不得默认写 /etc、/usr/local/bin 或 /etc/systemd/system；
真实 VPS 操作必须在任务中明确授权；
每次变更必须运行与改动范围匹配的测试；
sources/ 为只读参考材料。
```

---

## 四、开发任务协议

每个 Codex 任务都应使用相同的任务说明结构。

```markdown
# 开发任务：<任务名称>

## 目标节点

## 关联需求 ID

## 必读文档

## 本次目标

## 允许修改

## 不得修改

## 允许的副作用

## 不允许的副作用

## 必须通过的测试

## 是否允许真实 Debian 13 VPS 操作
- 否 / 是（明确范围）

## 完成条件

## 若发现契约问题
- 停止扩大范围；
- 说明冲突；
- 提出契约变更；
- 不直接绕过基线。
```

### 4.1 单任务粒度

一个任务应只处理：

```text
一个节点；
或一个已定义的编排流程；
或一个明确的故障修复；
或一项有清晰验收条件的测试补充。
```

不应在同一任务内同时完成“配置工具 + 部署流程 + 菜单 + 真实 VPS 调试”。这样才能知道每次改变了什么，也能在出现问题时准确定位。

### 4.2 任务结束时的固定汇报

每个任务结束时，Codex 应报告：

```text
完成了什么；
涉及哪些需求 ID 与节点；
修改了哪些文件；
运行了哪些测试及其范围；
没有测试什么；
是否触碰了真实 VPS；
发现了哪些待决问题或契约风险；
需求追踪矩阵应更新到什么状态。
```

---

## 五、真实 Debian 13 VPS 的操作授权

### 5.1 默认规则

在没有明确授权时，Codex 只能：

```text
编辑本地项目文件；
运行本地隔离测试；
运行 mock、dry-run 与故障注入；
生成计划和验收步骤；
检查本地静态输出。
```

在没有明确授权时，Codex 不得：

```text
连接 VPS；
安装软件包；
写 /etc；
创建或修改 systemd unit；
修改防火墙；
申请、续期或清理证书；
下载/替换活动协议二进制；
重启、停止或卸载真实服务。
```

### 5.2 真实操作任务应额外写明

```text
VPS 目标和连接方式；
本次允许修改的明确资源；
前置快照或备份位置；
预期影响；
回滚方案；
验收步骤；
停止条件；
是否需要需求方用 iOS Surge 确认连接。
```

### 5.3 分级授权

| 级别 | 允许动作 | 示例 |
|---|---|---|
| A | 只读 | 查看系统信息、日志、端口、配置摘要 |
| B | 隔离写入 | 在测试目录生成配置、执行 dry-run |
| C | 可恢复真实变更 | 安装脚本管理的 unit、测试端口、候选二进制，且已有快照 |
| D | 高影响真实变更 | 证书切换、活动服务更新、卸载、真实防火墙改动 |

每次任务默认最多 A/B；C/D 必须在任务说明中明确授权，不能由 Codex 自行推断。

---

## 六、测试执行规则

### 6.1 本地测试前提

```text
使用临时目录，不污染 /etc、/usr/local/bin、/etc/systemd/system；
外部命令通过替身或 mock 模拟；
测试密钥必须是专用假值；
测试输出不得包含真实主配置、证书或 iOS Surge 密钥；
失败时保留必要诊断，但不保留敏感内容。
```

### 6.2 变更后的最低测试

| 改动 | 最低测试 |
|---|---|
| Core | unit + 受影响 node |
| Python YAML 工具 | unit + N1 node + 原子失败 |
| Config/State/Revision | node + 修订恢复集成 |
| Registry/Adapter | node + 协议集成 |
| Resource primitive | node + dry-run + 相关故障注入 |
| Transaction/Orchestrator | integration + fault + smoke |
| 菜单 | smoke + 受影响编排流程 |
| 真实资源逻辑 | 以上测试 + 经授权的 Debian 13 验收 |

### 6.3 测试结论表达

不得只写“all tests passed”。至少应写：

```text
通过的层次：unit / node / integration / fault / smoke / real；
覆盖的需求 ID；
未覆盖部分；
真实验证是否执行；
是否仍存在已知风险。
```

---

## 七、首个实际开发任务

### 7.1 任务名称

```text
N1：Python YAML 配置工具
```

### 7.2 任务范围

**关联需求 ID**：

```text
REQ-CONFIG-001
REQ-STATE-001（仅为后续接口预留，不实现完整修订管理）
REQ-TRANS-001（仅为后续原子写入与快照提供基础）
```

**本次要实现**：

```text
最小有效 YAML schema；
读取、校验、查询、patch 和原子写入；
临时文件与权限控制；
非敏感错误输出；
隔离目录测试；
dry-run 不写入验证。
```

**本次不得实现**：

```text
正式菜单；
协议配置；
systemd、证书、防火墙、下载；
真实 VPS 操作；
完整部署、更新、卸载或回滚流程；
完整修订版 UI。
```

**必须验证**：

```text
合法 YAML 读取；
损坏 YAML 拒绝；
schema 不兼容；
合法 patch；
非法 patch；
临时写失败与替换失败保留旧文件；
默认输出脱敏；
dry-run 不修改文件。
```

**真实 VPS 授权**：否。

### 7.3 N1 完成后的预期状态

```text
需求追踪矩阵：REQ-CONFIG-001 可更新为 implemented/tested（取决于测试完成情况）。
节点契约：N1 的实际接口、测试位置和已知限制被补充。
下一任务：N2 配置、状态与修订管理。
```

---

## 八、文档与状态回写规则

### 8.1 需求追踪矩阵

每完成一个节点或编排流程，更新对应需求的交付状态：

```text
not-designed → designed → implemented → tested → verified
```

不能因为代码存在就直接标记 `verified`；必须有目标 Debian 13 验收记录。

### 8.2 节点契约

实现发现可接受的细节时，可以补充：

```text
实际接口；
测试文件或场景；
明确错误分类；
已验证限制。
```

若发现职责、输入输出或调用方向需要改变，必须先创建契约变更记录，不能只改代码。

### 8.3 技术决策基线

只有以下变化才更新技术决策基线：

```text
支持环境变化；
YAML/Python 边界变化；
协议注册表或状态模型根本变化；
事务与回滚规则变化；
二进制、证书、防火墙或真实验收策略变化。
```

---

## 九、切换到 Codex 的最终检查

```text
[x] 产品需求基线已形成。
[x] 技术决策基线已冻结。
[x] 需求追踪矩阵已建立。
[x] 开发模块及定位已完成。
[x] 首批节点契约已完成。
[x] 测试、故障注入与 Debian 13 验收计划已完成。
[x] Codex 本地开发交接规则已完成。
[ ] 需求方明确切换到 Codex 本地开发。
[ ] 创建实际代码工作区。
[ ] 建立项目级 AGENTS.md 与测试入口。
[ ] 创建 N1 开发任务并开始实现。
```

达到上述状态后，开发前文档阶段结束。后续应在 Codex 本地工作区中按照任务协议实现、测试和调试，不再在需求讨论中直接堆叠代码或临时修改产品规则。
