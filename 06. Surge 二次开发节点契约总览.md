
> 本文档定义 Surge 一键安装脚本二次开发的首批节点契约。节点契约是需求与代码之间的最小开发单位：它明确一个节点负责什么、不负责什么、接收什么、产出什么、失败时如何表达，以及必须通过哪些验证。

**状态**：开发前节点契约基线。  
**更新时间**：2026-07-26  
**适用版本**：Surge 一键安装脚本二次开发 v1

---

## 一、定位与使用规则

### 1.1 上游关系

```text
产品需求基线
→ 技术决策基线
→ 需求追踪矩阵
→ 开发模块及定位
→ 本节点契约总览
→ Codex 本地实现、测试与调试
```

节点契约不能改变已冻结产品行为。发现上游文档无法实现或自相矛盾时，必须先提出需求变更或技术契约变更。

### 1.2 什么是一个节点

一个节点应当同时满足：

```text
有清晰职责；
有稳定输入与输出；
可独立测试；
被多个流程复用，或承担重要风险；
拆出来后能降低复杂度。
```

不为几行简单 helper、只使用一次的薄包装或尚未稳定的未来抽象建立独立契约。

### 1.3 开发任务的最低要求

Codex 开发任何节点前，任务说明必须写明：

```text
目标节点；
关联需求 ID；
允许修改的范围；
不得修改的范围；
必须通过的自动化测试；
是否允许在 Debian 13 VPS 上执行真实动作；
若发现契约不合理时的处理方式。
```

在当前“开发前文档”阶段，不创建节点代码、测试脚本或真实 VPS 资源。

---

## 二、统一契约约定

### 2.1 结果语义

每个节点返回的结果，无论 Bash 内部函数、Python 辅助工具还是资源调用，概念上都必须能表达：

```text
result：success / skipped / failed
category：validation / dependency / external / conflict / interrupted / rollback
summary：面向用户或调用方的简短说明
details：供日志与诊断使用的非敏感细节
changed：本节点是否已修改资源
recoverable：是否存在可执行恢复动作
```

具体返回码、JSON、临时文件或 Bash 数据结构由实现决定，但不得依赖不受控的 `eval`，也不得将敏感值写入普通日志。

### 2.2 dry-run 约定

所有可能修改系统、文件或外部资源的节点必须支持由调用方传入的 dry-run 上下文。

```text
dry-run 可以计算计划、验证可预先验证的输入、生成预览；
dry-run 不得写主配置、替换活动文件、调用 systemctl、修改防火墙、下载并安装二进制、申请证书；
dry-run 的成功不等于真实执行成功；
结果必须明确标注为 dry-run。
```

### 2.3 权限约定

```text
主配置、修订快照、备份和含密钥的派生文件至少为 600；
目录权限和所有者应限制普通用户读取敏感内容；
任何节点都不得将密码、PSK、私钥或完整客户端密钥写入默认日志；
用户明确请求 Surge 配置时，才向终端输出必要敏感参数。
```

### 2.4 状态约定

```text
desired：用户希望采用的设置。
applied：已健康验证并提交的设置与资源版本。
observed：最近从系统读取到的事实摘要。
```

节点本身不应随意混写三类状态：

```text
适配器只构建内容；
资源节点只报告执行事实；
事务层记录执行过程；
编排器决定何时请求 Config & State 提交 desired/applied/observed。
```

---

## 三、首批节点依赖图

```text
N1 Python YAML 配置工具
        ↓
N2 配置、状态与修订管理 ─────────┐
        ↓                         │
N3 操作事务、快照与回滚 ──────────┼──→ 后续 Deploy / Update / Uninstall 编排
                                  │
N4 协议注册表 ──→ N8 Snell 适配器 ┤
        ↓                         │
N5 二进制资源 ────────────────────┤
N6 systemd 服务原语 ──────────────┤
N7 防火墙资源 ────────────────────┘
```

N1-N3 是可靠性底座；N4-N7 是可复用描述与系统资源；N8 用于验证协议适配器如何接入公共边界。

证书资源、AnyTLS 适配器、Hysteria2 适配器、Surge 完整导出和业务编排器，在首批契约稳定后继续补充。

---

## 四、节点目录

| ID | 节点 | 类型 | 关联 P0 需求 | 主要依赖 |
|---|---|---|---|---|
| N1 | Python YAML 配置工具 | 配置辅助工具 | REQ-CONFIG-001 | Python 3、python3-yaml |
| N2 | 配置、状态与修订管理 | 配置状态层 | REQ-CONFIG-001、REQ-STATE-001 | N1、Core |
| N3 | 操作事务、快照与回滚 | 事务层 | REQ-TRANS-001、REQ-STATE-001 | N2、Core、资源节点 |
| N4 | 协议注册表 | 静态能力层 | REQ-SNELL-001、REQ-ANYTLS-001、REQ-HY2-001、REQ-BIN-001、REQ-FW-001 | Core |
| N5 | 二进制资源管理 | 资源原语 | REQ-BIN-001、REQ-UPDATE-001 | N2、N3、N4 |
| N6 | systemd 服务原语 | 资源原语 | REQ-SVC-001、REQ-TRANS-001 | Core、N3 |
| N7 | 防火墙资源管理 | 资源原语 | REQ-FW-001 | Core、N4、N3 |
| N8 | Snell v6 适配器 | 协议适配器 | REQ-SNELL-001、REQ-SURGE-001 | N4、Core |

---

## 五、N1：Python YAML 配置工具

### 5.1 目标

提供主 YAML 的唯一结构化读写入口，使 Bash 编排层可以安全读取、校验、查询和应用配置 patch，而不依赖文本替换。

### 5.2 负责

```text
读取 YAML；
校验 YAML 语法和 schema_version；
执行受限字段路径查询；
执行结构化 patch 合并或字段更新；
生成临时配置文件；
校验临时配置文件；
原子替换正式配置文件；
返回非敏感、机器可消费的结果。
```

### 5.3 不得负责

```text
调用 systemctl、journalctl、acme.sh、ufw 或 nft；
下载、安装或替换协议二进制；
创建或删除证书；
自行决定部署、更新或卸载步骤；
将操作标记为 applied success；
将完整敏感字段输出到普通 stdout 或日志。
```

### 5.4 输入

| 输入 | 含义 | 规则 |
|---|---|---|
| 配置路径 | 当前 YAML 文件路径 | 只能是调用方已验证的受控路径 |
| 操作类型 | read / validate / query / patch / atomic-write | 只能是白名单操作 |
| schema 版本 | 预期配置结构版本 | 不兼容时明确失败 |
| 字段路径 | 允许读取/修改的配置路径 | 禁止任意代码表达式或 shell 注入 |
| patch | 结构化字段变更 | 由调用方提供、由工具校验 |
| 输出策略 | 是否脱敏、是否写临时文件 | 默认脱敏、默认不泄露密钥 |

### 5.5 输出

```text
查询结果或校验结果；
字段缺失、类型不符或 schema 不兼容的明确错误；
临时文件或原子替换成功的状态；
被修改字段的非敏感摘要；
不包含密码、PSK、私钥的诊断信息。
```

### 5.6 副作用

- `read`、`validate`、`query` 无副作用。
- `patch` 仅在调用方明确要求写入、且事务达到提交点时才执行原子替换。
- 临时文件必须位于受控目录、具有严格权限，并在成功或失败后按策略清理。

### 5.7 失败处理

| 场景 | 结果 |
|---|---|
| YAML 语法错误 | `failed / validation`，不改原文件 |
| schema 不兼容 | `failed / validation`，要求迁移或明确升级 |
| 字段路径不允许 | `failed / validation`，不执行修改 |
| patch 类型非法 | `failed / validation`，不执行修改 |
| 临时文件写入失败 | `failed / external`，保留原文件 |
| 原子替换失败 | `failed / external`，保留原文件并返回路径 |

### 5.8 必测场景

```text
合法 YAML 读取与查询；
损坏 YAML 拒绝；
schema_version 不兼容；
合法 patch 后字段正确更新；
非法字段路径或类型拒绝；
写临时文件失败时原文件不变；
替换失败时原文件不变；
默认输出不泄露敏感字段；
dry-run 不写文件。
```

### 5.9 验收条件

- Bash 不再需要通过 `sed`、`awk`、文本替换等方式修改主 YAML。
- N2 可以依赖本工具完成所有主配置读写。
- 自动化测试覆盖成功与失败写入路径。

---

## 六、N2：配置、状态与修订管理

### 6.1 目标

将主 YAML 暴露为稳定的配置状态接口，管理 desired/applied/observed、历史摘要、敏感字段脱敏，以及最近 5 个成功修订版。

### 6.2 负责

```text
配置初始化；
调用 N1 读取和校验 YAML；
构建和验证 config patch；
提供 desired/applied/observed 查询接口；
保存操作摘要和状态；
创建、列出、清理成功修订版；
为事务提供当前状态快照与恢复材料；
确保敏感文件权限与展示脱敏。
```

### 6.3 不得负责

```text
自行启动、停止或重启服务；
自行生成协议 runtime、unit 或 Surge 内容；
自行决定端口冲突、证书依赖或防火墙影响；
在事务健康验证前更新 applied；
把历史修订直接复制到活动路径并称为恢复完成。
```

### 6.4 输入

| 输入 | 含义 |
|---|---|
| 当前配置路径 | 受控主 YAML 路径 |
| 配置 patch | 已由编排器计算的 desired 或状态变更 |
| 操作摘要 | 类型、目标、时间、非敏感变更摘要 |
| 修订材料 | 主配置、受影响派生文件、二进制/资源摘要 |
| 展示策略 | 状态页、预览页或完整客户端导出上下文 |

### 6.5 输出

```text
当前 desired/applied/observed 状态摘要；
规范化配置 patch；
修订列表与修订元数据；
可供 N3 使用的快照清单；
脱敏配置展示模型；
历史摘要写入结果。
```

### 6.6 状态规则

```text
desired：可在事务开始前保存为用户意图或待应用状态，但不得伪装为成功。
applied：只有 N3 报告健康验证成功后，才能由 N2 提交。
observed：由只读系统观察结果更新，必须记录检查时间。
history：只记录有限摘要，不保存完整原始命令输出。
```

### 6.7 修订版规则

```text
高影响操作成功后，保存一个成功修订版；
最多保留最近 5 个；
第 6 个成功修订创建后清理最旧一个；
进行中事务快照不计入 5 个；
修订版包含配置、受影响派生文件、资源版本和恢复说明；
敏感内容与主配置采用同等级权限；
恢复某个修订必须由 N3 执行，不由 N2 直接完成。
```

### 6.8 失败处理

| 场景 | 结果 |
|---|---|
| 配置不存在 | 可初始化或返回明确未初始化状态 |
| 配置损坏 | 拒绝继续写入，保留诊断上下文 |
| 快照创建失败 | 高影响事务不得继续 |
| 成功修订保存失败 | 返回明确失败；由事务决定是否标记待处理 |
| 清理旧修订失败 | 不删除新修订，记录维护告警 |
| 脱敏规则缺失 | 默认更严格地隐藏字段 |

### 6.9 必测场景

```text
初始化空配置；
desired/applied/observed 分别更新且互不混淆；
敏感字段在状态和预览中脱敏；
创建 6 个成功修订后只保留最新 5 个；
修订元数据含必要恢复材料；
快照失败阻止后续事务；
历史摘要不包含完整密钥；
权限设置正确。
```

### 6.10 验收条件

- 状态页可区分用户期望、已应用和真实观察状态。
- 历史修订可被列出并交给后续恢复编排器。
- 任何读取主配置的模块都通过 N2，而不是自行解析 YAML。

---

## 七、N3：操作事务、快照与回滚

### 7.1 目标

为部署、配置应用、更新、卸载、证书申请/续期和修订恢复提供统一的安全执行骨架，确保成功状态只在验证后提交，失败时优先恢复当前可运行状态。

### 7.2 负责

```text
获取和释放全局高影响操作锁；
校验操作计划；
协调预览与确认前后的事务边界；
创建临时快照；
按计划执行步骤并记录完成情况；
调用健康验证；
请求 N2 提交 applied、历史和成功修订；
按逆序调用恢复动作；
记录 rollback-success、failed 或 dirty 状态；
生成供结果层显示的完成/未完成/修复建议摘要。
```

### 7.3 不得负责

```text
从菜单读取用户输入；
自行决定协议组合、证书依赖或端口冲突；
自行拼接 Snell、AnyTLS 或 Hysteria2 的配置；
直接实现下载、systemd、防火墙或证书命令；
在没有编排器提供计划时执行高影响操作。
```

### 7.4 输入：操作计划

操作计划至少必须包含：

```text
operation_id 与操作类型；
目标协议和资源；
用户可见影响摘要；
前置校验；
执行步骤；
每步的成功判定；
每步的恢复动作；
最终健康验证；
Surge 导出是否必需；
不强制回滚但必须记录的失败；
提交 desired/applied/observed 和成功修订的条件。
```

### 7.5 输出

```text
success / partial-success / failed / rollback-success / dirty；
已完成步骤；
失败步骤及非敏感原因；
恢复结果；
当前状态摘要；
人工修复建议；
关联 operation_id 与日志位置。
```

### 7.6 事务规则

```text
acquire lock
→ validate plan
→ display preview
→ explicit confirmation
→ create snapshot
→ mark pending
→ execute steps
→ health verify
→ commit applied state
→ export Surge if required
→ write history and successful revision
→ release lock
```

失败时：

```text
stop later steps
→ run required restore actions in reverse order
→ verify restored state
→ mark rollback-success / failed / dirty
→ record repair advice
→ release lock
```

Surge 导出失败、防火墙同步失败或历史摘要失败等不适合强回滚的场景，必须在计划中显式声明；不得由实现者临时判断。

### 7.7 中断与并发

```text
同一时刻只允许一个高影响操作；
第二个操作必须被拒绝并指出活动 operation_id；
Ctrl+C 或异常中断时记录最后完成步骤；
若可安全恢复，执行恢复；否则置 dirty 并输出修复建议；
不得因中断直接删除唯一旧备份。
```

### 7.8 必测场景

```text
计划缺少恢复动作时拒绝执行；
锁已占用时拒绝第二个操作；
快照失败时不执行任何活动资源修改；
中间步骤失败时逆序恢复；
健康检查失败时不提交 applied；
Surge 导出失败保留已验证服务端结果并记录未同步；
恢复成功与恢复失败状态不同；
中断后不丢失操作上下文。
```

### 7.9 验收条件

- 所有高影响编排器只能通过 N3 修改活动资源。
- applied 成功状态不再在下载、写文件或 restart 前提前写入。
- 一个故障注入测试可以证明旧服务或明确 dirty 状态被正确保留。

---

## 八、N4：协议注册表

### 8.1 目标

建立所有协议差异的受控描述来源，使菜单、校验、适配器、二进制、防火墙、服务和测试共享同一套协议能力信息。

### 8.2 负责

```text
协议 ID 与显示名；
协议网络类型、默认端口和端口资源规则；
是否需要 TLS、引用哪种证书策略；
二进制资源与共享关系；
unit/runtime 标识；
参数定义：类型、默认值、必填性、敏感性、校验与依赖规则；
健康检查描述；
对应适配器构建器标识。
```

### 8.3 不得负责

```text
保存用户实际密码、PSK、端口、域名、带宽等；
直接提供终端菜单；
写主配置；
执行下载、防火墙或 systemd；
用动态字符串执行任意函数或 eval。
```

### 8.4 v1 最小协议描述

| 协议 | TLS | 网络 | 参数重点 | 限制 |
|---|---|---|---|---|
| Snell v6 | 否 | TCP 单端口 | PSK、客户端地址、mode | mode 仅 default |
| AnyTLS | 是，共享证书 | TCP 单端口 | 密码、TFO、reuse | 共享资源兼容检查 |
| Hysteria2 | 是，共享证书 | UDP 单端口 + UDP 范围 | 密码、跳跃、Gecko、带宽 | 主端口不落入跳跃范围 |

### 8.5 输入与输出

**输入**：已验证的协议 ID、可选能力查询。  
**输出**：只读协议 spec、参数 spec、资源声明与健康检查描述。

未注册协议必须返回 validation error，不允许落入默认分支。

### 8.6 必测场景

```text
三个 v1 协议均包含必填字段；
未注册协议被拒绝；
Snell 声明 TCP 而不是 UDP；
Hysteria2 声明 UDP 与可选 UDP 范围；
TLS 需求与共享证书规则正确；
敏感参数被标记为敏感；
注册表缺字段时启动校验失败。
```

### 8.7 验收条件

- 防火墙、二进制、适配器和菜单不再各自硬编码协议核心能力。
- 协议定制参数可由同一注册表被展示、校验和构建逻辑引用。

---

## 九、N5：二进制资源管理

### 9.1 目标

负责协议二进制的受控版本选择、官方获取、完整性校验、临时试运行、安装、备份和恢复，避免未经验证的“最新版本”直接破坏活动服务。

### 9.2 负责

```text
根据资源 ID 和 Debian 13 amd64 获取官方候选版本；
筛选三个最新的具体兼容正式版本；
展示版本号、稳定性标签和发布日期；
下载到临时目录；
校验 checksum 或签名；
解压并定位可执行文件；
执行版本或帮助检查；
备份、安装、替换、恢复与缓存；
返回版本、来源、校验和和路径摘要。
```

### 9.3 不得负责

```text
自行选择用户要安装哪个版本；
直接重启服务；
直接写 applied 成功状态；
决定批量更新停止策略；
绕过共享资源兼容检查；
在无可信完整性信息时安静继续安装。
```

### 9.4 输入

| 输入 | 含义 |
|---|---|
| binary resource ID | 来自 N4 的资源标识 |
| 已启用消费者 | 用于共享资源兼容性判断 |
| 用户选择版本 | 必须是已展示候选之一 |
| 当前版本与活动路径 | 用于备份、替换和恢复 |
| dry-run | 仅展示候选与计划，不下载/安装 |

### 9.5 输出

```text
三个明确候选版本；
下载、校验、解压、试运行结果；
已安装或已恢复的版本摘要；
当前二进制路径与版本元数据；
失败原因与是否可恢复。
```

### 9.6 失败处理

| 场景 | 处理 |
|---|---|
| 官方来源不可达 | 不改活动二进制 |
| 无可验证完整性信息 | 拒绝安装或返回明确风险状态 |
| checksum/签名失败 | 删除候选临时文件，不改活动二进制 |
| 解压或文件定位失败 | 不改活动二进制 |
| 版本检查失败 | 不替换活动二进制 |
| 服务验证失败 | 由 N3 请求恢复旧二进制与服务 |

### 9.7 必测场景

```text
候选只包含三个具体版本；
不兼容架构或协议版本被排除；
校验失败不替换旧文件；
压缩包错误不替换旧文件；
版本检查失败不替换旧文件；
共享二进制兼容性不满足时无法选择；
恢复旧版本后元数据正确。
```

### 9.8 验收条件

- 安装和更新不再依赖“用户提供裸可执行 URL”。
- 真实 Debian 13 上可从官方来源选择具体版本、校验、安装并检测版本。

---

## 十、N6：systemd 服务原语

### 10.1 目标

提供 systemd unit 的安全安装/恢复、服务控制、状态观察和有限日志读取能力。它是资源原语，不拥有业务流程。

### 10.2 负责

```text
unit 临时写入、权限设置和原子替换；
unit 备份与恢复；
daemon-reload；
start / stop / restart；
enable / disable；
is-active / is-enabled；
有限行数 journal 摘要；
返回结构化服务观察结果。
```

### 10.3 不得负责

```text
决定本次哪些服务必须重启；
决定是否开启开机自启；
决定服务启动失败后的整体业务回滚；
修改主配置；
将 restart 命令返回成功等同于健康成功。
```

### 10.4 输入

```text
受控 unit 名称和 unit 内容；
明确 action；
日志行数上限；
dry-run 标志；
调用方已确定的服务目标。
```

### 10.5 输出

```text
unit 写入/恢复结果；
systemctl 动作结果；
active 与 enabled 状态；
有限日志摘要；
非敏感失败原因；
是否已改变 systemd 资源。
```

### 10.6 必测场景

```text
unit 写入失败保留旧 unit；
daemon-reload 失败可被上层识别；
start/restart 失败不伪装 active；
is-active 与 is-enabled 分别观察；
日志行数被限制；
dry-run 不调用 systemctl；
恢复旧 unit 后可重新 reload。
```

### 10.7 验收条件

- 所有 systemd 操作只有该节点执行。
- N3 可依据该节点的观察结果参与健康验证。
- Debian 13 systemd 上完成 unit 安装、观察、重启、启停与日志验证。

---

## 十一、N7：防火墙资源管理

### 11.1 目标

将协议网络需求转换为明确的 TCP、UDP 或 UDP 范围规则计划，并在 manual/auto 模式下安全执行或输出指引。

### 11.2 负责

```text
检测支持的本机工具；
验证网络资源计划；
渲染 manual 模式指引；
在 auto 模式为 ufw 或 nftables 应用已支持规则；
报告云安全组仍需用户确认；
报告规则应用成功、失败或无法自动处理。
```

### 11.3 不得负责

```text
从协议名称猜测传输类型；
同时默认开放 TCP 与 UDP；
决定某次操作是否需要端口；
默认删除旧规则；
在不支持的工具上猜测命令；
将本机规则成功误报为云安全组已放行。
```

### 11.4 输入

```text
由 N4 与编排器生成的网络资源计划；
manual / auto 模式；
已检测到的工具能力；
是否为临时 TCP 80 需求；
dry-run 标志。
```

### 11.5 输出

```text
规范化网络计划；
manual 指引或 auto 执行结果；
已新增规则摘要；
未处理或失败规则；
云安全组提示；
是否可恢复的规则上下文。
```

### 11.6 失败处理

| 场景 | 处理 |
|---|---|
| manual 模式 | 不改规则，始终成功输出指引 |
| 工具不支持 | 返回可读的 manual 降级结果 |
| auto 写入失败 | 返回 external failure，由计划决定是否继续或提示人工处理 |
| 范围非法 | validation failure，不执行任何规则 |
| 云安全组未知 | 不伪装成功，保留显式提示 |

### 11.7 必测场景

```text
Snell 计划只产生 TCP；
AnyTLS 计划只产生 TCP；
Hysteria2 主端口产生 UDP；
Hysteria2 跳跃产生 UDP range；
manual 不执行系统命令；
不支持工具降级 manual；
auto 失败结果可被 N3 识别；
不生成自动删除旧规则计划。
```

### 11.8 验收条件

- 不会重现“所有协议同时开放 TCP 和 UDP”的问题。
- Debian 13 上实际规则与监听协议一致；云安全组仍有明确提示。

---

## 十二、N8：Snell v6 适配器

### 12.1 目标

以 Snell v6 作为第一个协议适配器，验证注册表、配置、二进制、systemd、防火墙、事务和 Surge 导出之间的边界是否可用。

### 12.2 负责

```text
读取 Snell 协议 spec 和 desired 配置；
校验端口、PSK、客户端地址类型和 mode；
构建 Snell runtime 内容；
构建 Snell systemd unit 描述；
构建 Snell Surge 条目；
声明 Snell 需要的 TCP 网络资源、二进制和健康检查预期；
构建对 desired 的协议 patch。
```

### 12.3 不得负责

```text
读取终端输入；
下载或安装 snell-server；
写 runtime 或 unit 文件；
调用 systemctl；
修改防火墙；
写主配置；
申请证书；
决定事务成功或失败；
为 AnyTLS 或 Hysteria2 做任何业务决定。
```

### 12.4 输入

| 输入 | 规则 |
|---|---|
| 端口 | 合法范围、与已占资源不冲突 |
| PSK | 满足 Snell v6 既定长度和格式规则 |
| 客户端地址类型 | `domain` 或 `ip` |
| 客户端地址 | 域名或合法 IP；IP 场景不要求 TLS 域名 |
| mode | v1 只能为 `default` |
| binary/runtime/unit 路径 | 来自受控资源与注册表 |

### 12.5 输出

```text
runtime content；
unit spec；
Surge entry；
config patch；
resource requirements：snell-server、TCP port、service health spec；
参数校验错误。
```

### 12.6 协议规则

```text
Snell v6 不需要 TLS 证书；
网络资源仅为 TCP 单端口；
仅 Snell 且客户端使用 IP 时，主域名可为空；
mode 字段保留，但 v1 仅支持 default；
普通状态与预览中 PSK 必须脱敏；
Surge 导出时使用主配置中可恢复的真实 PSK。
```

### 12.7 必测场景

```text
合法 domain 与 IP 地址类型；
非法端口、PSK、IP、domain 和 mode 拒绝；
runtime 中 listen、PSK、mode 和既定字段正确；
unit 使用受控 binary 与 runtime 路径；
Surge 条目格式正确；
网络资源仅为 TCP；
不调用 systemd、防火墙、证书或配置写入；
输入缺失时返回明确 validation error。
```

### 12.8 验收条件

- N8 可在无菜单、无真实系统命令的隔离环境中构建并测试。
- 其输出可由后续 Deploy 编排器交给 N5/N6/N7/N3 执行。
- 真实部署阶段能在 Debian 13 + iOS Surge 上验证 Snell 连接，但这不是 N8 单独完成的责任。

---

## 十三、后续节点契约队列

首批节点稳定后，按依赖补充：

| 顺序 | 节点 | 主要关联需求 |
|---|---|---|
| 1 | Certificate resource + certificate workflow | REQ-CERT-001 |
| 2 | AnyTLS adapter | REQ-ANYTLS-001 |
| 3 | Hysteria2 adapter | REQ-HY2-001 |
| 4 | Runtime writer 与 Surge exporter | REQ-SURGE-001 |
| 5 | Deploy orchestrator | REQ-DEPLOY-001 |
| 6 | Config-apply orchestrator | REQ-CONFIG-APPLY-001 |
| 7 | Update orchestrator | REQ-UPDATE-001 |
| 8 | Uninstall orchestrator | REQ-UNINSTALL-001 |
| 9 | Revision-restore orchestrator | REQ-STATE-001 |
| 10 | 正式菜单与交互流程 | REQ-UX-001、REQ-RESULT-001 |

每新增节点前，先检查是否真的需要独立契约；若只是当前节点内部简单 helper，应留在当前节点内。

---

## 十四、进入 Codex 本地开发前的检查

```text
[x] 技术决策基线已冻结。
[x] P0/P1 需求已追踪。
[x] 模块树、调用边界和事务责任已确定。
[x] 首批 8 个公共高风险节点已定义契约。
[ ] 为每个首批节点建立具体的开发任务与允许修改范围。
[ ] 建立本地代码工作区、AGENTS.md 和测试入口。
[ ] 选择第一个实现节点：N1 Python YAML 配置工具。
```

建议切换至 Codex 后的第一项实际开发任务是 N1。它不操作 VPS 服务，却为后续所有配置、状态、修订和事务能力提供唯一可靠入口。
